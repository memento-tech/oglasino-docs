# State

The single source of truth for "where are we." Mastermind reads this at the start of every planning session. Engineer agents read the relevant slice. Docs/QA keeps it current.

**Last updated:** 2026-06-01
**Target:** regression testing in 1–2 weeks, production in 1 month

---

### Google Analytics v1

- **Spec:** [features/google-analytics-v1.md](features/google-analytics-v1.md)
- **Status:** `shipped`
- **Branch:** `dev`
- **Why active:** code-complete across nine engineer briefs (2026-05-22 to 2026-05-23) on `dev`. All 12 events plus automatic `page_view` wired in `oglasino-web` and verified end-to-end via DebugView 2026-05-23. Backend `wasRegister` field shipped in `oglasino-backend`. Stage GA4 admin (`oglasino-stage-49abb`, `G-P0LEVEJ0V9`) fully configured. Prod GA4 admin (`oglasino-prod-7e5db`, `G-GNKB4WBNC0`) configured 2026-05-23 with two post-launch items deferred (see below).

**Tasks remaining (post-launch):**
- Mark four conversion events on prod GA4 property once they arrive post-deploy: `sign_up`, `product_create_completed`, `contact_seller_clicked`, `message_sent`. GA4 requires at least one event of each type to exist before allowing conversion marking. Natural trigger: first day or two of production traffic.
- Verify `oglasino.com` in Google Search Console (DNS TXT record via Cloudflare, ~10-15 min). Recommended timing: 1-2 days before prod deploy. Then 1-2 weeks post-launch, come back to GA4 → Admin → Product links → Search Console links → Link, connecting Search Console to the `oglasino-web-prod` data stream.
- Legal-drafts chat notification: Privacy Policy paragraph review for GA4's cookieless-ping behavior under `analytics_storage: denied`.
- Mobile (`oglasino-expo`) adoption queued in the Expo backlog as its own future Mastermind chat.

Stale `2026-05-19-oglasino-web-ga4-discovery-1.md` archived 2026-05-23 from `oglasino-web/.agent/` to `oglasino-docs/sessions/`.

---

## Feature pipeline

### Feature statuses

- `planned` — spec exists, no engineering started
- `in-progress-backend` — backend work active or required next
- `backend-stable` — backend done, contract frozen, web can adopt
- `in-progress-web` — web work active
- `web-stable` — web done, mobile can adopt
- `in-progress-mobile` — mobile work active
- `mobile-stable` — all three platforms shipped to feature branch
- `shipped` — merged to main, in production (or staging if pre-launch)
- `parked` — deliberately deferred

---

## Active features

### SEO foundation

- **Spec:** [features/seo-foundation.md](features/seo-foundation.md)
- **Status:** `shipped`
- **Branch:** `dev`
- **Why active:** code-complete across eighteen web and backend briefs (2026-05-19 to 2026-05-24) on `dev`. All critical-class SEO defects resolved: JSON-LD delivery as `<script>` tags, per-base-site hreflang clusters with per-cluster x-default, `<html lang>` BCP-47 tags, server-side breadcrumbs, product cards as `<a href>` anchors, heading hierarchy, sr-only catalog SEO descriptions, small fixes batch (apex canonical, user canonical with userId, robots.txt locale-aware patterns, og:image translated alts, publisher.logo URL, priceCurrency uppercase, metadataBase override removal), JSON-LD removed from privacy/terms, localized URL paths abandoned, hreflang verification script (`npm run test:hreflang`).

**Tasks remaining:** Schema.org Validator verification owed by Igor on three pages (product, privacy [now empty], terms [now empty]). Six deferred items handed off to `future/` documents (image alt translation, per-locale legal content, intro cluster splitting, per-category authored descriptions, active submission to Google, free-zone OG image). Item A of the deferred batch (free-zone OG image semantic correction) shipped 2026-05-25 — see `decisions.md`. Items B (intro cluster splitting) and C (per-category descriptions) remain deferred per the handoff at `.agent/handoffs/seo-deferred-batch.md`.

---

### Expo structural foundation

- **Spec:** [features/expo-structural-foundation.md](features/expo-structural-foundation.md)
- **Status:** `shipped` (code)
- **Branch:** `dev`
- **Why active:** four sequenced Mastermind chats (Φ1 auth lifecycle, Φ2 navigation, Φ3 performance, Φ4 service layer + error contract) that establish the structural floor for `oglasino-expo` before feature-adoption chats A–I open. The structural audit (2026-05-24) found 26 structural issues across seven categories; most cross-cut feature chats and must be solved at the foundation.

**Tasks remaining:** none — all four foundation chats are shipped (Φ1 2026-05-25, Φ2 2026-05-27, Φ3 2026-05-29, Φ4 2026-05-29; each its own Mastermind chat per Part 10, each on `decisions.md`). The Expo structural foundation program is complete; the A–I feature queue now opens against it, chat A (mobile validation rebuild) first since it was blocked on Φ4's service layer.

### Expo auth lifecycle (Φ1)

- **Spec:** [features/expo-auth-lifecycle.md](features/expo-auth-lifecycle.md)
- **Status:** `shipped` (code) / `verifying` (manual smoke pending on real devices per spec §10)
- **Branch:** `dev`
- **Why active:** code-complete across seven engineer briefs (2026-05-24 to 2026-05-25) on `dev`. All seven structural auth-lifecycle findings (F1, F2, F3, F4, F5, F13, F22) addressed in `oglasino-expo`. New `AccountStateDialogsInit` and `ForegroundRevalidationInit` components wired. 109 tests passing throughout. Q2 (backend USER_BANNED 403 response shape) verified via read-only backend chat — confirmed identical envelope across all three 403 paths.

**Tasks remaining:** manual smoke pending on real device per `features/expo-auth-lifecycle.md` §10 definition-of-done. Once smoke completes, status flips to `shipped`. Chat E (mobile user-deletion adoption) inherits the `AccountStateDialogsInit` scaffold and activates the `accountJustDeleted` subscription stub.

### Expo navigation foundation (Φ2)

- **Spec:** [features/expo-navigation-foundation.md](features/expo-navigation-foundation.md)
- **Status:** `shipped` (code) / `verifying` (manual smoke pending per spec §8)
- **Branch:** `dev`
- **Why active:** native `<Stack>` and `<Tabs>` navigators across all six layouts, BottomBar as `tabBar` prop, four `Dimensions.get` sites migrated to `useWindowDimensions`, `+not-found.tsx` custom TopBar, inline auth guards in `(secured)/`. Always-mounted Stack pattern adopted after expo-router constraint discovered during smoke (see decisions.md 2026-05-27).

**Tasks remaining:** manual smoke per spec §8 on real device. Once smoke clears, status flips to `shipped`.

### Expo performance foundation (Φ3)

- **Spec:** [features/expo-performance-foundation.md](features/expo-performance-foundation.md)
- **Status:** `shipped` (the cold-start boot-loading blocker was resolved by the `expo-boot-redesign` feature, which replaced the boot/HTTP layer wholesale — see [decisions.md](decisions.md) 2026-05-29 entry; real-device regression passed)
- **Branch:** `new-expo-dev` (Igor's deliberate isolation per the 2026-05-25 "Expo foundation work is tentative" decisions.md entry)
- **Why active:** code-complete across 22 engineer briefs (Briefs 1–19a, 2026-05-27 to 2026-05-28). The original Φ3 performance scope (Briefs 1–11a) shipped: F7 Zustand selector refactoring, F8 React.memo + renderItem stability, F9 staged expo-image migration, F10 AppContext split, F20 CategorySelector/CitySelector → SectionList, F23 chat-store split, F25 ProductList scroll throttle. The chat then absorbed a boot/HTTP-layer refactor (Briefs 12–19a) after manual smoke exposed a latent Φ2 pre-bootstrap gap: codes-derived `globalThis`-pinned bootstrap barrier in `apiStore.ts`, `<RequireBaseSite>` per-screen render gate, `api.ts ↔ authStore` require-cycle break, ProductList language-switch handling. The barrier, render gate, and cycle break all verified functioning; an unrelated cold-start boot-loading issue remains.

**Tasks remaining:** none for Φ3 itself. The cold-start boot-loading hang was resolved by the `expo-boot-redesign` feature (shipped 2026-05-29), which replaced `AppContext.bootstrap` with the `bootStore` gate state machine — the `apiStore` `globalThis` barrier, `<RequireBaseSite>`, and the `[BOOT]` diagnostic instrumentation that Φ3 added were all deleted by the redesign. See [decisions.md](decisions.md) 2026-05-29 entry. Real-device regression passed.

### Expo service layer + error contract (Φ4)

- **Spec:** [features/expo-service-error-contract.md](features/expo-service-error-contract.md)
- **Status:** `shipped` (code) / `verifying` (on-device airplane-mode check deferred to the Ψ chat)
- **Branch:** `new-expo-dev`
- **Why active:** the last of the four foundation chats, code-complete across three engineer briefs (2026-05-29) on `new-expo-dev`, behind a read-only Phase-2 audit. Brief 1: shared `parseServiceError` helper (`src/lib/utils/parseServiceError.ts`) + ~14 services converted from swallow-and-return-sentinel to surface-via-throw. Brief 2: `reportedReviewId` wire threaded button → dialog → body, the two dashboard review cards re-pointed, the `reportService` boolean-inversion fix, the two REVIEW codes surfaced as translated messages. Brief 3: `@react-native-community/netinfo` (11.4.1, SDK-54-pinned) + a connectivity Gate 0 ahead of the maintenance gate, a new `'offline'` BootStatus + offline screen + `OfflineReconnectInit`, honoring the three boot-redesign invariants. See [decisions.md](decisions.md) 2026-05-29 Φ4 entry.

**Tasks remaining:** on-device airplane-mode verification is the Ψ chat's job and depends on the pending iOS+Android rebuild (the new `@react-native-community/netinfo` native module must be in the binary — see Risk Watch). Chat A (mobile validation rebuild) inherits the surface-via-throw caller list (see the decisions.md carry-forward) and opens next.

### Expo release readiness

- **Spec:** [features/expo-release-readiness.md](features/expo-release-readiness.md)
- **Status:** `in-progress`
- **Branch:** `dev`
- **Why active:** triaged inventory of the gap between mobile (`oglasino-expo`) and the rest of the platform. Nine future Mastermind chats are queued (α, A–I) to close the gap. Chat α (admin removal) shipped 2026-05-24 — mobile no longer carries admin code; admin remains a web-only tool. Remaining chats run one at a time in the order Igor chooses; default sequencing in section 7 of the spec.

**Tasks remaining:** the queue has been revised by the 2026-05-24 structural audit. Four foundation chats (Φ1–Φ4) from [`features/expo-structural-foundation.md`](features/expo-structural-foundation.md) run before A–I. Full order: Φ1 → Φ2 → Φ3 → Φ4 → A → B → C → D → E → H → G → F → I → Ω → Ψ. A–I are now smaller because foundation work absorbs cross-cutting concerns. All four foundation chats (Φ1–Φ4) are now shipped (Φ4 2026-05-29); the A–I queue opens. Next up: chat A (mobile validation rebuild), which was blocked on Φ4's service layer.

### Expo cloud setup

- **Spec:** [infra/expo/cloud-setup.md](infra/expo/cloud-setup.md)
- **Pre-deploy checklist:** [infra/expo/pre-deploy-checklist.md](infra/expo/pre-deploy-checklist.md)
- **Status:** `shipped` (development tier code-complete and validated on physical device)
- **Branch:** `new-expo-dev` (uncommitted; Igor's deliberate isolation strategy per the 2026-05-25 "Expo foundation work is tentative" decisions.md entry)
- **Why active:** new Expo cloud project (`382cac59-45fa-420e-ab80-7fc709e6d2f3`) created to replace previously-deleted broken cloud project. Three-tier build profile structure (`development` / `preview` / `production`) with per-tier bundle IDs (`com.oglasino.development` / `com.oglasino.preview` / `com.oglasino`), per-tier Firebase Apps registered in stage and prod Firebase projects, EAS Workflow YAMLs in place for auto-build on push to `preview` / `main` branches. SHA-1 and SHA-256 fingerprints from the development tier's keystore registered in `oglasino-stage-49abb` for `com.oglasino.development` Android app. Firebase email/password authentication validated end-to-end on both iOS and Android physical devices.

**Tasks remaining:**
- Final Android + iOS rebuild capturing all post-first-build changes (firebaseClient.ts env-var guard, per-tier name + scheme, updated google-services.development.json with fingerprints)
- Google Sign-In validation on both platforms (post-rebuild)
- All items in [infra/expo/pre-deploy-checklist.md](infra/expo/pre-deploy-checklist.md) before any preview or production build ships to testers / users

### Expo Maintenance Split

- **Spec:** [features/expo-maintenance-split.md](features/expo-maintenance-split.md)
- **Status:** `shipped` (developed across all four repos 2026-05-29 → 05-30; end-to-end stage rehearsal passed, Igor-confirmed 2026-05-31)
- **Branches:** router `stage`, backend `dev`, expo `new-expo-dev`, web `stage`
- **Why active:** splits the single `maintenance.active` Cloudflare KV flag into two dependency flags (`maintenance.web.active`, `maintenance.backend.active`), composed per-client at the worker; routes mobile API traffic through a worker-only `/api/mobile/*` label gated on backend availability via the existing edge-cached liveness probe (re-pointed at dependency-aware `/actuator/health/readiness`); makes Cloudflare KV the sole maintenance store (removes the backend maintenance DB config + the `/public/maintenance/active` poll); and folds in the admin-toggle move to `/api/secure/` + `@PreAuthorize`. Shipped across router/backend/expo/web (21 sessions); §9 end-to-end stage rehearsal passed.

**Tasks remaining:** none blocking — feature is shipped. **Owed:** the paired `decisions.md` supersession entry (PART 2 of the close-out sweep, superseding the 2026-05-15 single-flag entry) — Mastermind-drafted, not yet available. Status was flipped on Igor's direct confirmation of the rehearsal (2026-05-31); the 2026-05-15 `decisions.md` entry stays accurate-as-history until the supersession entry lands.

### Backend calls reduction (mobile)

- **Spec:** [features/backend-calls-reduction-mobile.md](features/backend-calls-reduction-mobile.md)
- **Status:** `shipped` (code) / mobile half `verifying` (on-device deferred to Ψ)
- **Branch:** mobile `new-expo-dev`, backend `dev`
- **Why active:** chat I (the §I queue item) closed 2026-05-31. Fresh Phase-2 audit confirmed predecessors absorbed most of §I. Shipped: C1 (drop dead `idToken`), C2 (cache `getUserForId`), C4 (skip per-login Firestore read) on mobile; C5 (cache `findActiveBaseSiteCodes`, no-TTL Redis, all clients) on backend. Per-pathname version check confirmed already gone. See [decisions.md](decisions.md) 2026-05-31 entry.

**Tasks remaining:** mobile C1/C2/C4 on-device verification (Ψ), shares the pending iOS+Android-rebuild device dependency. Backend C5 is shipped on `dev` (710 tests green). `connection-pool-hardening` retains the `@Transactional`/single-flight items on the same base-site path — unchanged by this feature.

### Consent Mode — Mobile

- **Spec:** [features/consent-mode-mobile.md](features/consent-mode-mobile.md)
- **Status:** `shipped` (code-complete and merged-ready on `new-expo-dev`; on-device runtime verification — string resolution + privacy-link navigation against a seeded build — carried by the Ψ pass, like this branch's other `verifying` items)
- **Branch:** `new-expo-dev`
- **Why active:** chat **G** in the Expo release-readiness queue ([features/expo-release-readiness.md](features/expo-release-readiness.md) §5), now merged with chat **C** (consent-field cleanup) because both touch `app/owner/dashboard/user.tsx`. Spec authored 2026-05-30 (merged C+G Mastermind chat) behind three read-only audits (expo/web/backend). Part C cleans the settings screen (removes `allowPreferenceCookies` end-to-end, two dead cookie-label sections, dead `ConsentData`/`GlobalCookie` types, the B2/B16/B17 fixes, a save-failure message); Part G builds a single-axis device-local "Allow analytics" consent decision + `isAnalyticsConsentGranted()` gate + first-run prompt + settings toggle. Deliberately not a port of web's four-signal cookie consent (mobile has no cookies, no SSR, no Google Consent Mode). See [decisions.md](decisions.md) 2026-05-30 entry.

**Tasks remaining:** on-device runtime verification (Ψ) — string resolution for the seven `mobile.consent.*` COOKIES keys and privacy-link navigation against a seeded build; depends on the pending iOS+Android rebuild (see Risk Watch). Native-translator review of the seven placeholder `mobile.consent.*` RS/RU/CNR keys (the COOKIES native-review pool grows by 7 — seeded by `oglasino-backend-consent-mode-mobile-1`, 2026-05-31; tracked in Risk Watch). Privacy-policy stored-data disclosure remains a dependency on the queued lawyer review (not authored here). Chat F (mobile analytics) is the gate's only consumer and opens separately. See [decisions.md](decisions.md) 2026-05-31 entry.

### Product validation

- **Spec:** [features/product-validation.md](features/product-validation.md)
- **Status:** `web-stable` / mobile **code-complete** on `new-expo-dev`, pending Ψ on-device smoke test — **do not promote to `mobile-stable` until Ψ passes**.
- **Branch:** `feature/validation-refactor` (backend/web); `new-expo-dev` (mobile)
- **Why active:** backend and web are complete and manually tested by Igor. Mobile (Expo) adoption is now **code-complete** on `new-expo-dev` across briefs A1–A5 plus a cleanup brief, a URL-parity brief, and translation-seed sessions — the product create/edit/validation surface rebuilt onto the frozen contract (combined `addUpdate` retired for `create`/`update`/`pre-validate`; update payload allow-listed via `toUpdateWirePayload`; client validation reduced to structural-only; per-field server errors via `parseServiceError`). A mobile-vs-web delta audit confirms behavioral parity with web except items Igor is deferring to the post-smoke pass. See [decisions.md](decisions.md) 2026-05-30 Chat A entry.

**Tasks remaining:** on-device smoke test (Ψ) is the last gate before `mobile-stable`. Product create exercises the image-upload pipeline, so product Ψ depends on the pending iOS+Android rebuild landing first (see Risk Watch). Igor adds `issues.md` entries after the smoke test; deferred items (the `copy.done.label` RU/CNR native-translator review; the `finish.label` dead-key/rename question; the un-run `new.product.success.*` / DIALOG locale-completeness sweeps; the Ω cleanup leftovers) wait for that post-smoke pass.

### QA Preparation

- **Spec:** [features/qa-preparation.md](features/qa-preparation.md)
- **Status:** `in-progress-web`
- **Branch:** `feature/qa-preparation`
- **Why active:** schema, renderer, and 12 portal page topics are done and approved by Mastermind. Remaining work: 10 owner-dashboard page topics, then portal feature/flow topics. Continuing in a fresh Mastermind chat after handoff.

**Tasks remaining:** 10 owner-dashboard page topics (authored at the simplified voice from the start); 1 deferred feature/flow topic (`report-flow`); 1 stale-content cleanup on the `global-header-search` topic in `app/[locale]/design/topics.ts` — flip the Enter-key "Known issue" pitfall and matching `qaChecklist` standing-pin to reflect the 2026-05-20 fix of the `issues.md` 2026-05-16 entry.

### User Deletion

- **Spec:** [features/user-deletion.md](features/user-deletion.md)
- **Manual test cases:** [features/user-deletion-test-cases.md](features/user-deletion-test-cases.md)
- **Status:** `shipped` (code) / `verifying` (manual smoke pending on full surface)
- **Branch:** `feature/user-deletion`
- **Why active:** code-complete across backend (13 sessions, 487 tests passing) and web (14 sessions, 154 tests passing). Self-service deletion with 7-day grace period, scheduled hard-delete cron, Firebase orphan reconciliation, ban-with-reason flow, 12-month re-registration prevention, banned-user content hiding, backend → web cache revalidation, and admin actions audit logging all live. Folded in the prior "Account-disabling & token-revocation enforcement" backlog item. Backlog-triage closed (21 of 21 items addressed; one item — B19 mobile parity — deferred to its own Mastermind chat post-merge).

**Tasks remaining:** manual smoke pending on full surface (blocked on messaging fix — see [issues.md](issues.md) 2026-05-19 entry). Lawyer review of legal drafts pending. Native-translator review of placeholder RS/CNR/RU translations pending. Mobile (`oglasino-expo`, `new-expo-dev`): **code-complete across Briefs 1–4, pending on-device Ψ** — foundation (store flags, `deleteCurrentUser`, DTO fields, C-6 listener guard); Danger Zone + reauth/confirm dialog + delete→sign-out sequence; post-deletion dialog activation + grace-period/banned chat badge + PENDING_DELETION/BANNED send-gate + notices; Φ1 banned-key fix. **NOT `mobile-stable`** — Ψ on-device verification owed (shares the pending iOS+Android rebuild dependency; see Risk Watch). Expo-backlog row → mobile `in-progress`.

### Messaging

- **Spec:** [features/messaging.md](features/messaging.md)
- **Status:** `shipped` (code) / `verifying` (cleanup cron live-smoke pending on first prod Sunday)
- **Branch:** `feature/messaging`
- **Why active:** code-complete across rules (`oglasino-firestore-rules` Brief 1: 24 rule tests passing on emulator + stage-deployed + smoke-verified), web (`oglasino-web` Briefs 2/3/6a-B: 200 tests passing), backend (`oglasino-backend` Briefs 4/5/6a-A: 549 tests passing, cleanup cron + admin bug fix + endpoint cleanup + translation seeds). First-message permission-denied production defect fixed (`get()` → `getAfter()`), `productId` no longer dropped on first message, send-failure toast surfaced, chat list "Load more" affordance, auto-linkified URLs, admin chat-list receiver bug fixed, notification create rule locked to `if false`, public `/api/public/notification/test` endpoint removed, Sunday cleanup cron deployed with sentinel-based anonymization for hard-deleted users.

**Tasks remaining:** cleanup cron live-smoke pending on first production Sunday at 03:00 UTC (tracked in [issues.md](issues.md) 2026-05-20 entry); admin welcome chat for new users (carried as a future Mastermind chat, sketch in [features/messaging.md](features/messaging.md) §11.1); mobile (`oglasino-expo`) adoption now **code-complete on `new-expo-dev`** pending Ψ on-device verification (Expo-backlog row `in-progress`; see [decisions.md](decisions.md) 2026-05-31 entry — do not flip to `mobile-stable` until Ψ).

### Cookies closing

- **Spec:** [features/cookies-closing.md](features/cookies-closing.md)
- **Status:** `shipped`
- **Branch:** `feature/cookies-closing` (off `dev`)
- **Why active:** four-item cleanup pass after Consent Mode v2. Items 1 (card-size button fix), 2 (`isDashboard` → `portalScope` migration, including `dashboard` → `owner` rename), and 3 (`userPreferenceService` removal) shipped as spec'd. Item 4 shipped the language portion with significant divergence from the original spec (routing locale vs formatter locale split, navigation wrappers, cookie-clearance-on-decline). Theme portion of Item 4 shipped separately as the [theme-cookie-persistence](#theme-cookie-persistence) feature (2026-05-27). Closing details in [decisions.md](decisions.md) 2026-05-22 entry; permanent reference for the routing + language + cookie infrastructure at [features/routing-and-language.md](features/routing-and-language.md).

**Tasks remaining:** none — theme work (Path A) shipped 2026-05-27 as the [theme-cookie-persistence](#theme-cookie-persistence) feature. No backend, router, or Firestore Rules adoption queued (web-only feature).

### Image alt text translation

- **Spec:** [features/image-alt-translation.md](features/image-alt-translation.md)
- **Status:** `shipped`
- **Branch:** `feature/image-alt-translation` (off `dev`)
- **Why active:** code-complete across two backend sessions and one web session (2026-05-25) on `feature/image-alt-translation`. Four keys (`intro.image.alt`, `about.hero.image.alt`, `intro.og.image.alt`, `intro.meta.description`) seeded in the `METADATA` namespace across EN / RS / RU / CNR (16 seed rows). Five code sites swapped in three files (`app/page.tsx`, `about/page.tsx`, `generateIntroPageMetadata.ts`). Spec amended mid-feature to fold in the C2 description finding (Serbian `description` literal shipping to all locales in search results). Three read-only audits (web audit 1, web base-site audit, backend category-catalog audit) confirmed all four images are base-site-neutral and the translation schema has no base-site dimension.

**Tasks remaining:** native-translator review of placeholder SR / CNR / RU values across the four keys.

### Theme cookie persistence

- **Spec:** [features/theme-cookie-persistence.md](features/theme-cookie-persistence.md)
- **Status:** `shipped`
- **Branch:** `feature/theme-cookie-persistence` (off `dev`)
- **Why active:** code-complete across two engineer sessions (Brief 1 and Brief 1b) on `feature/theme-cookie-persistence` 2026-05-27. Replaces `next-themes` with a cookie-based theme system. Manual smoke verified by Igor. Tri-state toggle (light/system/dark) shipped in `PortalConfigDialog`. `globalCookie.theme` widened to include `'system'`. Closes the deferred theme portion of Item 4 from the cookies-closing chat.

**Tasks remaining:** none — feature is complete. Mobile (`oglasino-expo`) has its own theme story and is not in scope; if mobile cookie persistence is desired later, queue as a separate Expo chat.

### Consent Mode v2

- **Spec:** [features/consent-mode-v2.md](features/consent-mode-v2.md)
- **Status:** `shipped`
- **Branch:** `feature/consent-mode-v2` (off `dev`)
- **Why active:** code-complete across `oglasino-web` (foundation + SSR snippet + banner rebuild + footer "Cookies" entry + dedicated `/owner/cookies` settings page + card-size cookie gating + legacy banner / `firebaseAnalytics.ts` / `PREFERENCE_COOKIE_NAME` cleanup) and `oglasino-backend` (25 translation keys seeded across EN/RS/RU/CNR via inline-append; `allowPreferenceCookies` mirror removed end-to-end from column, DTOs, entity, and propagator call sites). Browser-local `og_consent` is the single source of truth; no backend mirror. Igor verified the consent UX manually post-SSR-cache-resolution on 2026-05-21.

**Tasks remaining:** native-translator review of placeholder RS / RU / CNR copy across 25 keys (matches the 2026-05-19 User Deletion precedent). Privacy Policy review queued for the legal-drafts chat per the closing `decisions.md` 2026-05-21 entry. Follow-up work — `userPreferenceService` tracking-surface removal, language + theme as preference cookies, cross-device consent sync — queued in [`.agent/handoffs/consent-mode-v2-followups.md`](.agent/handoffs/consent-mode-v2-followups.md) for the next Mastermind chat.

### Review Reports

- **Spec:** [features/review-reports.md](features/review-reports.md)
- **Status:** `web-stable`
- **Branch:** backend `dev` / web `stage`
- **Why active:** code-complete across backend + web 2026-05-29. Backend built the missing review-reporting half — `ReportType.REVIEW`, validated `reportedReviewId` on the DTO + `Report` entity + V1 CHECK/column, a REVIEW service branch (existence + null-safe reviewer-only self-report, server-side), 3-way per-target dedupe, two new `ReportErrorCode` constants + 8 seed rows. Web swapped the `ReceivedReviewCard` mount to `reportedReviewId`, threaded the field through `ReportButton`/`ReportDialog`, and surfaced the two REVIEW codes as translated messages. Closes the 2026-05-28 `issues.md` REVIEW-wire entry (now `fixed`). See [decisions.md](decisions.md) 2026-05-29 entry.

**Tasks remaining:** native-translator review of RS / RU / CNR for the two new `report.reported_review*` keys (backend-seed-owned); end-to-end smoke + merge to staging. Mobile adoption was delivered by Φ4 (`expo-service-error-contract` brief 2, the review-report wire, 2026-05-29) — the standalone `oglasino-expo-review-reports` chat is absorbed, not opened. See [decisions.md](decisions.md) 2026-05-29 Φ4 entry.

### Error Code Domain Split

- **Spec:** [features/system-error-code-split.md](features/system-error-code-split.md)
- **Status:** `web-stable`
- **Branch:** backend `dev` / web `stage`
- **Why active:** code-complete across backend + web 2026-05-29. `ProductErrorCode` (47 constants) split behind a shared `ErrorCode` interface — 7 system codes → `SystemErrorCode`, 4 user/profile codes → `UserErrorCode`, 36 product codes retained; `ProductValidationException`/`ProductErrorResponse` widened to `ErrorCode`; `resolveTranslationKey` reworked to a fail-loud registry; 44 translation keys renamed in place (`product.system.*` → `system.*`, `displayName.*` → `user.display_name.*`, etc.); three new seed-coverage tests. Web renamed the one hardcoded `product.system.rate_limited` literal + four fixtures. Wire `code`/status byte-identical. See [decisions.md](decisions.md) 2026-05-29 entry.

**Tasks remaining:** merge to staging. No mobile adoption needed — backend-internal refactor + web string rename with no mobile-adoptable surface; mobile error handling is the separate Φ4 service-layer concern. Flag for Mastermind triage at mobile catch-up rather than asserting a backlog row.

### Expo System Theme

- **Spec:** [features/expo-system-theme.md](features/expo-system-theme.md)
- **Status:** `shipped` (code) / `verifying` (on-device Ψ pending)
- **Branch:** `new-expo-dev`
- **Why active:** mobile tri-state theme (light/system/dark) reaching parity with web. Code-complete across three sessions (`-1` audit, `-2` implementation, `-3` invented-key removal). App-owned choice/resolved store, `Appearance`-backed system resolution + live OS listener, ungated AsyncStorage persistence of the choice, boot hydrate/apply bridging the resolved value into NativeWind, icon-only 3-segment Sun/Monitor/Moon control in PortalConfigDialog, sidebar binary toggle replaced with a PortalConfigDialog opener. No backend work, no new translation keys (parity with web's icon-only control). See [decisions.md](decisions.md) 2026-06-01.

**Tasks remaining:** on-device Ψ (shares the pending iOS+Android-rebuild dependency — see Risk Watch). Do not promote to `mobile-stable` until Ψ passes.

---

## Backlog

Each becomes its own `features/<slug>.md` when it goes active.

| Feature                           | Status    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Image pipeline](features/image-pipeline.md) | `web-stable` | Backend + web complete. Expo has to adopt. Spec migrated from `oglasino-backend/docs/16-image-pipeline.md` to `features/image-pipeline.md` on 2026-05-27.                                                                                                                                                                                                                                                                                                              |
| Backend calls reduction           | `shipped` | Closed via chat I, 2026-05-31. Mobile C1/C2/C4 + backend C5. See `features/backend-calls-reduction-mobile.md`. Now its own feature spec, not just a backlog line.                                                                                                                                                                                                                                                                                                                        |
| Firestore rules tightening        | `planned` | Security review and rule edits. Requires explicit Mastermind sign-off.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Chat & messaging cleanup          | `planned` | Needs audit before scoping.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Privacy Policy + Terms (drafts)   | `drafted` | Pre-lawyer drafts in `legal/privacy-policy-draft.md` and `legal/terms-of-use-draft.md`. Handoff package in `legal/lawyer-handoff.md`. Pending lawyer review and finalization before launch.                                                                                                                                                                                                                                                                                             |
| Connection-pool & cache hardening | `planned` | Structural follow-ups from the connection-pool incident: per-key single-flight dedup on concurrent cache misses, removing `@Transactional` from `getAllBaseSites`, the `redisBaseSites` 24h-TTL cold-cache trigger (warmup-gating closed the post-deploy window only), `RateLimitFilter` categorisation of public reference-data endpoints, and explicit `@Order` on the four `@Component` filters. See [features/connection-pool-hardening.md](features/connection-pool-hardening.md). |
| Catalog versioning | `planned` | Backend extension: expose a catalog version (header on the catalog response, or a separate endpoint). Web and mobile read the version and clear+refetch only on mismatch. Closes the audit's "once-only `initialized` flag persists stale catalog until app restart" concern (`audit-expo-structural.md` Finding 3) without forcing a refetch on every logout. Surfaced during Φ1 F3 verdict (2026-05-24) when the catalog clear was removed from `authStore.logout()` — catalog is not user-scoped and does not belong in logout cleanup. Backend audit chat is the natural place to scope. |

---

## Expo backlog

Tracks every feature that is `web-stable` or `shipped` on backend/web but not yet adopted on mobile. Docs/QA maintains it per the 2026-05-17 decision: append a row when a feature reaches `web-stable` or `shipped`; update or remove the row when mobile adopts the feature and its slug reaches `mobile-stable` on the feature spec.

Mobile adoption sessions are slugged per feature, not per time window. A session adopting `<feature-slug>` is `oglasino-expo-<feature-slug>-1`, `-2`, etc.

| Feature                                                                  | Web/Backend status | Mobile status | Adopted in session | Notes                                                                                                                                                          |
| ------------------------------------------------------------------------ | ------------------ | ------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Product validation](features/product-validation.md)                     | `web-stable`       | `in-progress` | `oglasino-expo-product-validation-1`..`-8` | Mobile **code-complete** on `new-expo-dev` (briefs A1–A5 + cleanup + URL parity + translation seeds), behaviorally at parity with web per the delta audit; on-device smoke (Ψ) pending before flip to `adopted`/`mobile-stable`. Do not promote until Ψ passes. See [decisions.md](decisions.md) 2026-05-30 Chat A entry. |
| [Product filtering and search](features/product-filtering-and-search.md) | `shipped`          | `in-progress` | `oglasino-expo-product-filtering-1`..`-9` + `filters-deadcode-console-cleanup-1` | Mobile **adopted via code review** on `new-expo-dev` (chat D): the five parity fixes + `applyRandom` suppression + the two mobile-only correctness fixes; on-device (Ψ) verification still pending — code-review; pending Ψ; see [issues.md](issues.md) 2026-05-31 entries. Two known open items: SELECT-filter dispatch (open); the dialog-refresh/pagination items are now fixed but unverified on-device. Do not promote to `adopted`/`mobile-stable` until Ψ passes. Spec `## Platform adoption` section authored 2026-05-31. See [decisions.md](decisions.md) 2026-05-31 chat-D entry. |
| [User Deletion](features/user-deletion.md)                               | `shipped`          | `in-progress` | `oglasino-expo-user-deletion-1`..`-4` | Mobile **code-complete** on `new-expo-dev` across Briefs 1–4 (foundation: store flags, `deleteCurrentUser`, DTO fields, C-6 listener guard; Danger Zone + reauth/confirm dialog + delete→sign-out; post-deletion dialog activation + grace-period/banned chat badge + PENDING_DELETION/BANNED send-gate + notices; Φ1 banned-key one-line fix). On-device Ψ pending before flip to `adopted`/`mobile-stable` — the consumer-facing subset (Cases 1, 2, 3, 5, 11, 15, 16, 17) per [features/user-deletion.md](features/user-deletion.md) §21.8; admin cases N/A (consumer-only). Shares the pending iOS+Android-rebuild device dependency (see Risk Watch). Do not promote until Ψ passes. See [issues.md](issues.md) 2026-05-31 entries. |
| [Image pipeline](features/image-pipeline.md)                             | `web-stable`       | `adopted` | `oglasino-expo-image-pipeline-1`..`-5` + `oglasino-expo-imagesourcesheet-liveness-1` + `oglasino-backend-image-source-sheet-1` | Implemented on `new-expo-dev` (off-process via `feature/image-pipeline-v2`), validated 2026-05-30 (110 tests pass), V6+V9 conformance fixes applied. C1 polish landed in session `-5` (progress text, dead-code deletion, `isPngInput` consolidation); B15 string swap landed (3 keys in `ImageSourceSheet.tsx` — backend seed `oglasino-backend-image-source-sheet-1`, mobile swap `oglasino-expo-imagesourcesheet-liveness-1`), completing chat H's code work → `adopted`. Remaining before `mobile-stable` = on-device smoke (Ψ) per [features/image-pipeline-mobile-test-cases.md](features/image-pipeline-mobile-test-cases.md), blocked on the pending iOS+Android rebuild (see Risk Watch). See [decisions.md](decisions.md) 2026-05-30 and 2026-05-31 (chat H) entries. |
| [Backend calls reduction](features/backend-calls-reduction-mobile.md)    | `web-stable`       | `in-progress` | `oglasino-expo-backend-calls-reduction-2`..`-4` | Mobile C1/C2/C4 code-complete on `new-expo-dev`; backend C5 shipped on `dev`. On-device Ψ pending before `adopted`/`mobile-stable`. See [decisions.md](decisions.md) 2026-05-31 chat-I entry.                                                          |
| [Messaging](features/messaging.md)                                       | `shipped`          | `in-progress` | `oglasino-expo-messaging-adoption-2`..`-5` | Mobile **code-complete** on `new-expo-dev` across five briefs (n=1 Phase-2 audit; `-2` store-core; `-3` screen+UI; `-4` Cycle B dyad; `-5` dialog deleted-peer guard), brought to parity with the frozen contract; on-device verification (Ψ) pending before flip to `adopted`/`mobile-stable`. Do not promote until Ψ passes. Uses no new native module but shares the pending iOS+Android-rebuild device dependency (see Risk Watch). See [decisions.md](decisions.md) 2026-05-31 entry. |
| [Google Analytics v1](features/google-analytics-v1.md)                  | `shipped`          | `not-started` |                    | Web code-complete and stage + prod admin configured 2026-05-23. Two prod-side items deferred to post-launch: conversion-event marking once events arrive, and Search Console verification + GA4 link. Mobile adoption opens its own `oglasino-expo-google-analytics-v1-<n>` chat per the 2026-05-17 slug decision. Mobile has its own SDK story (`@react-native-firebase/analytics` or `expo-firebase-analytics`) plus App Tracking Transparency on iOS. |
| [Image alt text translation](features/image-alt-translation.md)         | `shipped`          | `not-started` |                    | Web-only metadata feature (intro page alt text, about hero alt, intro og:image alt, intro meta description). Mobile may have equivalent surfaces (intro/about/landing screens with their own image and metadata stories) but that is separate work, not direct adoption — flag for Mastermind to triage at the time of mobile catch-up. |
| [Version checksums](features/version-checksums.md)                       | `shipped`          | `mobile-stable` | `oglasino-expo-boot-redesign-1`..`-14` | Mobile now consumes `/api/public/versions` via Gate 4 of the `expo-boot-redesign` boot state machine; cold-start freshness is checksum-driven. Shipped 2026-05-29 (see [decisions.md](decisions.md)). The per-NS-per-language `/versions` upgrade is tracked separately via [features/version-checksums-per-language.md](features/version-checksums-per-language.md), not this row. |
| [Version checksums (per language)](features/version-checksums-per-language.md) | `shipped`          | `mobile-stable` | `oglasino-expo-version-checksums-per-language-1`..`-2` | Per-(namespace, language) `/versions` checksums shipped 2026-05-29 (see [decisions.md](decisions.md)). Backend on `dev`, mobile on `new-expo-dev`, coordinated deploy. Replaces the per-NS-collapsed transitional contract from the boot-redesign feature (which adopted the predecessor per-NS-collapsed `version-checksums`, the row above). Mobile adopted in `oglasino-expo-version-checksums-per-language-2` (`-1` is the read-only Phase 2 audit). |
| [Review Reports](features/review-reports.md)                             | `web-stable`       | `adopted` | `oglasino-expo-service-error-contract-3` | Mobile review-report wire (`ReportType.REVIEW` + `reportedReviewId`) **adopted via the Φ4 foundation chat** [expo-service-error-contract](features/expo-service-error-contract.md) §4 (brief 2, 2026-05-29) — the standalone `oglasino-expo-review-reports` chat was absorbed, not opened (per that spec §4.4). Row stays until `mobile-stable`: remaining is RS/RU/CNR native-translator review of the two `report.reported_review*` keys (backend-seed-owned), end-to-end smoke, and merge. |
| [System theme](features/expo-system-theme.md) | `web-stable` | `in-progress` | `oglasino-expo-expo-system-theme-1`..`-3` | Mobile tri-state theme (light/system/dark) code-complete on `new-expo-dev`: app-owned choice/resolved store, Appearance-backed system resolution + live OS listener, ungated AsyncStorage persistence, boot hydrate/apply bridging resolved into NativeWind, icon-only 3-segment control in PortalConfigDialog, sidebar toggle → config opener. No backend work, no new keys (parity with web). On-device Ψ pending before `adopted`/`mobile-stable`. See [decisions.md](decisions.md) 2026-06-01. |

Mobile status values: `not-started`, `in-progress`, `adopted`.

---

## Decisions log

Decisions live in [decisions.md](decisions.md). Reference only.

---

## Session log

Most recent engineer sessions, newest first. Full archives in `sessions/`.

- **2026-06-01** — Bug chat: 2026-06-01 Expo UI/UX batch (continued). Code-complete on `oglasino-expo` `new-expo-dev` + `oglasino-web` + `oglasino-backend` `dev`: Danger Zone styling, AI-overlay opacity, empty-category centering, reviews inter-card spacing, dead given-card action row removed (mobile + web), reviews layout (clipped-last-card + full-width), self-follow button gated to non-owner, and two backend translation-seed fixes (`category.products` `{value}` un-escaped; `navigation.search.*` doubled/backslashed display-quotes normalized to a single pair). All on-device confirmation owed (folds into the pending iOS+Android rebuild / Ψ). One new deferred issues.md entry (review-deletion backend route). Web given-card removal staged on `dev` — branch-vs-`stage` confirmation owed before commit. Sessions archived: see below.
- **2026-06-01** — `oglasino-docs` bug-chat close-out: four bugs fixed in code, flipped `open → fixed` in `issues.md` with resolution notes — bug 1 `regionAndCity` update-product crash (`oglasino-expo`, `new-expo-dev`, Ψ-pending; sourced region/city from `useAuthStore().user`, matching web), bug 4 stale single-flag maintenance refs across 5 backend docs pages + the 06-architecture KV read/write under-description, bug 5 `ReportDialog` unguarded `tErrors` guarded with `t.has`, bug 6 consent spec-vs-code drifts corrected in `features/consent-mode-v2.md` (gtag-update `preference` key dropped; never-built `cookieConsent → og_consent` migration shim scrubbed; `/owner/user` → `/owner/cookies` ×2). Bugs 2/3/7/8/9 left open (feature-sized or non-bugs). **Brief-vs-reality (Igor-adjudicated):** the bug-chat draft's *fourth* consent drift (stale `ConsentMode.tsx`/`consentSignals.ts`/`consentCookie.ts`/`consentState.ts` implementation-file pointers) was phantom `Read` content — those names are not in the spec and never were (`grep` + `git log -S`); Part 2d was a no-op and the bug-6 note records the phantom as such. The Risk Watch tool-reliability row was updated to the **fifth and sixth** instances (renumbered from the draft's stale "fourth occurrence" — the row already carried a 2026-06-01 expo flag-round fourth instance; tally now six across two repos), flagging that phantom file content reached a drafted config edit before being caught. No `decisions.md` entry (no precedent/contract changed). Archived 15 files to `sessions/` (13 named summaries + the bug-1/bug-5 read-only audit deliverables) and deleted the sources.
- **2026-06-01** — Bug chat: 2026-05-31 Ψ UI batch. Six items fixed on `oglasino-expo` `new-expo-dev` and device-confirmed by Igor (iOS + Android): seller-block share-button gate (parity fix + shared `BackButton` extraction), categories back button, Privacy/Terms stale-link re-point, SearchInput iOS text-clip, login floating-label resting offset (`basic/Input.tsx` + `Textarea.tsx`), PortalConfigDialog flag-round. System-theme support handed to a separate Mastermind chat (a build, not a bug-chat tweak). One adjacent finding (no `ProductUserDetails` render test) recorded in session summary only — no render-test infra in the expo suite, deliberately not added. Net new issues.md entries: 0. Six engineer session summaries archived to `sessions/` and sources deleted: seller-share-gate-1, categories-back-button-1, legal-link-targets-1, search-input-ios-clip-1, input-label-resting-offset-1, portal-config-flag-round-1. A seventh same-chat session (`ui-cleanup-adjacent-findings-1`, the adjacent-cleanup patch) was left un-archived pending Igor — not in the brief's archive list and it carries an owed-for-Mastermind/Ψ item (the iOS floating-label resting offset owes on-device re-derivation after Igor's `text-md`→`text-base` change).
- **2026-05-31** — `oglasino-docs` bug-batch follow-up (two Igor-confirmed edits from the close-out's flagged items): flipped the 2026-05-16 report-submit trust-boundary `issues.md` entry `open` → `fixed` (both halves complete — backend 2026-05-28, web error-code mapping 2026-05-31) and refreshed its inline web-mapping note to drop the now-stale "awaiting confirmation" flag; added a new forward-facing `state.md` Risk Watch note recording the `new-expo-dev` standing lint baseline drift to **84** warnings (0 errors), so future expo briefs stop false-flagging against the older **80** figure (the historical 80 measurement in the messaging row left untouched, as it was a correct bracketed record). Closes the bug chat's last config-file dependencies.
- **2026-05-31** — `oglasino-docs` 2026-05-31 bug-batch config close-out: applied the parallel bug-chat pass that worked nine bugs across four repos (router / backend / web / expo) in one pass (expo lane verification-only, zero code changes). `issues.md`: nine status flips with resolution notes — **fixed in code:** backend `allowPhoneCalling` write-path (`setAllowPhoneCalling` wired into `DefaultUserFacade.updateCurrentUserData`), backend representative admin `@PreAuthorize` behavioral test (`MaintenanceAdminControllerPreAuthorizeTest`; ~13 other admin controllers still annotation-only — scope caveat preserved), web `allowPreferenceCookies` dead type fields deleted, router stage HTML 503 now carries `NOINDEX_HEADER`; **missed-flip closed:** Φ3 cold-start boot-loading hang (resolved 2026-05-29 by `expo-boot-redesign`); **no-op / no-bug against current code:** web HEIC stage-label already uses the underscore `converting_heic` form (no bug), mobile SELECT-filter dispatch deferred (no `SELECT` member in app or backend `FilterType` enum), `ONE_MIN` host file deleted by `expo-boot-redesign`, `FilteredProductList` `searchText` already `?? ''`-guarded. Plus the 2026-05-16 report-submit trust-boundary entry's web error-code-mapping follow-up recorded complete (full `ReportErrorCode` enum surfaced as translated copy; overall-status flip left for Igor — see brief-vs-reality). One new low-severity `issues.md` entry (web `ReportDialog` unguarded `tErrors()` with no missing-key fallback). No `state.md` feature-pipeline or Expo-backlog row changes; §4 lint-baseline correction skipped (see brief-vs-reality). Bug chat's closure-gate dependency.
- **2026-05-31** — `oglasino-docs` chat-I close-out (`backend-calls-reduction-mobile`): authored `features/backend-calls-reduction-mobile.md`; applied the decisions.md chat-I entry; flipped the 2026-05-31 "active base sites" issues.md entry to `fixed` with a two-part resolution note; updated state.md (new active-features block, backlog row → `shipped`, Expo-backlog row → mobile `in-progress`). Archived six session files (2 audits + 4 impl summaries) and deleted sources.
- **2026-05-31** — `oglasino-docs` image-pipeline chat H final closeout (`oglasino-docs-image-pipeline-2`): recorded B15 shipping (backend seeded 3 `image.source.*` keys; mobile swapped the 3 hardcoded strings), completing chat H's code work. Four edits across three config files. **(1)** New `state.md` Risk Watch row — native-translator review of the three `image.source.*` placeholders (RU ×3 / CNR ×3 = 6 placeholder values; EN+RS final), same precedent as Consent Mode v2 / User Deletion / image-alt-translation. **(2)** Image-pipeline Expo-backlog row flipped mobile `in-progress` → **`adopted`** (NOT `mobile-stable` — Ψ on-device smoke still owed, blocked on the pending iOS+Android rebuild); Adopted-in-session cell extended with the B15 sessions (`oglasino-backend-image-source-sheet-1` seed + `oglasino-expo-imagesourcesheet-liveness-1` swap); Notes cell rewritten. **(3)** New `decisions.md` 2026-05-31 entry at top (above the AppState/sweeper entry) — chat H closed, what shipped (progress text `-5`, B15 strings, dead-code deletion, `isPngInput` consolidation), the dropped non-problems, and the two cross-repo facts logged for a future docs reconciliation (review-`scope` enum has no REVIEW member; seed carries 29 keys/116 rows vs the spec's stated 17/68). **(4)** `features/image-pipeline.md` § Platform adoption — new bullet noting the image-source picker strings are now translation-keyed; top-line status stays `web-stable`. **Brief-vs-reality:** the brief predicted the mobile-swap session would be in the `oglasino-expo-image-pipeline-…` series; the actual swap was session `oglasino-expo-imagesourcesheet-liveness-1` (a separate slug) — used the real name per the brief's "use the actual session names" instruction. No `mobile-stable` flip; no existing Risk Watch row removed. Three B15 engineer summaries (+ two audits) sit un-archived in the expo/backend `.agent/` folders — flagged for Igor (archival not in this brief's scope).
- **2026-05-31** — `oglasino-docs` image-pipeline chat H Phase E docs/config closeout (`oglasino-docs-image-pipeline-1`): applied five deliverables. **E-a:** created the missing `features/image-pipeline-mobile-test-cases.md` (gate-2/Ψ artifact, 14 on-device cases across the four upload surfaces + display + failure paths; referenced by spec/state/decisions but never on disk until now). **E-b:** corrected the false "Created the on-device smoke test-case doc" claim in the 2026-05-30 session-log entry — reworded to "referenced … (file itself created later, 2026-05-31, chat H Phase E)". **E-c:** new `decisions.md` 2026-05-31 entry at top — mobile relies on the backend orphan sweeper for abandoned uploads (no `AppState` teardown), matching web's zero-client-cleanup pattern; sweeper-timing note (03:00 Europe/Belgrade, not UTC; 24h grace is the load-bearing figure). **E-d:** flipped two 2026-05-30 `issues.md` entries `open` → `fixed` (mobile dead code `ProductReviewImageImport.tsx`/`app/__smoke__/upload.tsx`; duplicate `isPngInput`), each with a chat-H session `-5` resolution note. **E-e:** added the progressive-status-text bullet to `features/image-pipeline.md` § Platform adoption (As-built mobile, via the `stageLabel` helper, session `-5`) — top-line status stays `web-stable`; updated the Image-pipeline Expo-backlog Notes cell (C1 polish landed in `-5`; remaining-before-`adopted` = B15 string swap; remaining-before-`mobile-stable` = Ψ) and bumped the session range to `..-5`. **No `adopted`/`mobile-stable` flip** (B15 string swap + Ψ both pending); no native-review Risk Watch row added (B15 keys not yet seeded). No engineer session summaries to archive this chat.
- **2026-05-31** — `oglasino-docs` mobile messaging adoption close-out (`oglasino-docs-messaging-2`): mobile adopted the frozen [`features/messaging.md`](features/messaging.md) contract, **code-complete on `new-expo-dev`**, pending Ψ — across five engineer briefs behind a Phase-2 audit (n=1 `audit-messaging-adoption.md`; `-2` store-core: atomic 4-op existing-chat batch incl. the missing chat-root `lastMessage`/`lastUpdated` write, send/delete throw, `getActiveChat` counterparty from `users[]`, mark-seen after-commit, `deleteChat` Firestore-first, `tempProductReason→tempProductContext`; `-3` screen+UI: deleted-peer fallback + crash guard, send/delete-failure toasts, block badge, chat-list load-more, five hardcoded strings → existing keys, URL linkify; `-4` Cycle B dyad break; `-5` `ChatUserFunctionsDialog` deleted-peer guard). `decisions.md`: new 2026-05-31 closing entry at top (parity behavior; five banked decisions — no new translation seeds, forward block-doc field `blockedUserId`, store-throws/screen-toasts UX, delete-toast reuses `messages.send.failed.toast`, Cycle B lazy-`require` over a new module; pending-Ψ posture; lint-confirm caveat). `state.md`: Messaging Expo-backlog row → mobile `in-progress` (NOT `mobile-stable`); Messaging active-feature Tasks-remaining updated; new Risk Watch row (Ψ shares the iOS+Android-rebuild device dependency though no new native module is added, + the Brief 3 lint-confirm note); this session-log line. `messaging.md`: two spec-drift corrections — §3.4 forward block-doc field `blockerId` → `blockedUserId` (web+mobile both write it; `blockerId` is reverse-index only); §5.11 stale seed-row IDs corrected to the on-disk EN 3391 / RS 5491 / RU 7591 / CNR 1291; §3.6 left untouched. `issues.md`: Cycle B (2026-05-28) flipped `open` → `fixed` with a resolution note; one new 2026-05-31 entry (spec §3.6 admin view-token override not implemented backend-side — open/medium, backend + web-admin surface, NOT mobile — for separate Mastermind triage). The delete-toast copy nuance was recorded as a decision (not an issue) and the `ChatUserFunctionsDialog` field-source observation lives in the Brief 5 summary only, per the don't-over-file/don't-hide call. Archived the four engineer session summaries (`oglasino-expo-messaging-adoption-2`..`-5`) plus the n=1 Phase-2 audit deliverable (`audit-messaging-adoption.md`) to `sessions/` and deleted the sources; the stale 2026-05-23 `audit-expo-readiness-messaging.md` (pre-build readiness audit, superseded) left in place per the consent-mode precedent.
- **2026-05-31** — `oglasino-docs` consent-mode-mobile close-out (`oglasino-docs-consent-mode-mobile-1`): feature shipped. `features/consent-mode-mobile.md`: rewrote § UI surfaces first-run prompt from the stale `intro-picker` slot to the as-built `ready`-gated overlay placement (with the two-reason justification — dead privacy link at `intro-picker` + existing users skipped); flipped status `not-started` → `shipped` (Ψ-pending caveat inline); added a Session log. `decisions.md`: new 2026-05-31 entry at top recording the F-facing gate contract (incl. "no in-repo caller yet, by design"), the deliberate `allowNotifications` dead-toggle gap, and the first-run-prompt placement amendment. `state.md`: `Consent Mode — Mobile` block → `shipped`/Ψ-pending status + updated Tasks-remaining; added a Risk Watch native-translator row for the seven new `mobile.consent.*` COOKIES placeholders (the COOKIES native-review pool +7); `Last updated` → 2026-05-31. `issues.md`: no change (the three audit-surfaced web/backend items stay logged-as-open, per the brief's out-of-scope). Archived the four build session summaries (expo C `-1`, expo G `-2`, expo G `-3`, backend seed `-1`) to `sessions/` and deleted the sources. **Brief-vs-reality:** the brief's §4 said to "increment the COOKIES figure by 7" *if* an explicit count exists; there is no single COOKIES native-review figure — consent-mode-v2's "25 keys" (its active-feature Tasks-remaining) is web-feature-specific, and the native-review queue lives as per-feature Risk Watch rows. Recorded the +7 as a new Risk Watch row (the "native-review queue note" the brief's parenthetical points at) rather than inflating v2's 25-key count, which would misattribute the seven mobile keys to the web feature. The stale `oglasino-expo/.agent/audit-expo-readiness-consent-mode.md` (2026-05-23, pre-merge, superseded) was left in place as before — not a build summary for this feature.
- **2026-05-30** — `oglasino-docs` Chat G (C+G merged, mobile consent) close-out: authored the new feature spec [`features/consent-mode-mobile.md`](features/consent-mode-mobile.md) verbatim from the merged C+G Mastermind draft (status `not-started`; fixed the one cross-reference to a correct relative link `consent-mode-v2.md`). `state.md`: new "Consent Mode — Mobile" active-features block (`not-started`, `new-expo-dev`) + this session-log line. `decisions.md`: new 2026-05-30 entry at top (single-axis device-local analytics consent model; F-facing gate contract; `allowNotifications` deliberately left untouched; C+G merge rationale; three-audit confirmation). `features/expo-release-readiness.md`: marked chat **C merged-into-G** in four places (Bucket-2 table, §5 C-section banner + G-section spec-authored banner, §7 sequencing items 8 + 12). `issues.md`: three new `open` entries from the audits (backend `allowPhoneCalling` write-path ghost — medium; web `allowPreferenceCookies` dead type fields — medium; web consent spec-vs-code drifts — low). Archived all six consent-mode-mobile deliverables to `sessions/` (expo audit + expo session summary, web audit + dated dupe, backend audit + dated dupe) and deleted the six sources. **Brief-vs-reality:** the brief named "four audit deliverables" but six files existed — web's and backend's dated session summaries are byte-identical to their `audit-*.md` deliverables (diff-confirmed); archived all six per the audit-archival precedent rather than dropping the dated records. The pre-merge chat-C "remove or wire `allowNotifications`" open question was reversed by the merge (now deliberately left alone) — reflected in the spec, decisions entry, and the release-readiness C-banner. One stale file left in place (out of scope): `oglasino-expo/.agent/audit-expo-readiness-consent-mode.md` (2026-05-23, superseded by the new expo audit).
- **2026-05-30** — `oglasino-docs` Chat A (product-validation mobile) close-out: added the `decisions.md` 2026-05-30 Chat A entry at top (mobile create/edit/validation rebuilt onto the frozen contract; six banked decisions — rebuild + allow-list narrowing, MIME-undefined-passes, `__system` option A, system/user keys, create-success UX, product-URL standardization; pending-Ψ posture). `state.md`: product-validation active-feature block → `web-stable` / mobile code-complete on `new-expo-dev` pending Ψ (NOT `mobile-stable`); Expo-backlog row → mobile `in-progress` (adopted in `oglasino-expo-product-validation-1..-8`); two Risk Watch rows extended (product-validation staged on `new-expo-dev`; product Ψ shares the iOS+Android-rebuild dependency since create exercises image upload); `Last updated` → 2026-05-30; this session-log line. `features/product-validation.md`: corrected six verified spec drifts — `free` field removed (§Request DTOs + §Known gaps), error-code enum attribution (42 across ProductErrorCode(36)+SystemErrorCode+UserErrorCode), `/api`-prefix normalization on five bare `/secure/products` paths, stale rate-limit key `product.system.rate_limited` → `system.rate_limited`, orphan-cleanup-on-any-non-success, filter-field step matching (all three forms); drift 7 (`button.close.label` DIALOG-vs-BUTTONS) skipped — spec doesn't track namespace placement. `issues.md`: UNTOUCHED (per Igor; issues wait for his post-smoke pass). Archived this chat's session artifacts to `sessions/` (8 expo product-validation summaries, the canonical + superseded mobile audits, 3 web create-flow audit summaries + the canonical + 2 superseded web audit deliverables, 4 backend audit/seed summaries) and deleted the sources. **Brief-vs-reality:** the brief asked to correct the image-pipeline Expo-backlog row from `not-started` to implemented-with-gaps, but that row was already corrected to `in-progress` by the 2026-05-30 image-pipeline close-out — left as-is, not duplicated.
- **2026-05-30** — `oglasino-docs` image-pipeline mobile close-out: brought the mobile half into the orchestration after a four-step validation chain (web ref audit → backend ref audit → mobile current-state audit → mobile deep validation) found it already implemented off-process on `new-expo-dev` (`feature/image-pipeline-v2`, squash `016da95`) and structurally conformant except two gaps, fixed in `oglasino-expo-image-pipeline-4` (V6 HEIC label localization, V9 avatar+chat orphan cleanup; tests 109→110). Added the `decisions.md` 2026-05-30 entry at top (off-process build, audit chain, V4 429-retry decision locked, honesty note). Reconciled `features/image-pipeline.md` against the as-built code: removed two stale "Deferred items" (view-token store, retry policy — both implemented), corrected the four divergent translation-key names to the seeded forms, added the live `review` scope, noted the `original` passthrough variant, rewrote Platform adoption to the shipped mobile reality. `state.md`: Image-pipeline Expo-backlog row → mobile `in-progress` (smoke pending); V6/V9-must-be-in-rebuilt-binary note added to the pending iOS+Android-rebuild Risk Watch row; this session-log line. `issues.md`: five new entries (5a/5b `fixed` — V6/V9; 5c/5d/5e `open` — web HEIC parity check, mobile dead code, duplicate `isPngInput`). Referenced an on-device smoke test-case doc `features/image-pipeline-mobile-test-cases.md` (the file itself was created later, 2026-05-31, in chat H Phase E). Archived the image-pipeline session summaries + reference audits from `oglasino-expo`/`oglasino-web`/`oglasino-backend` `.agent/` to `sessions/` and deleted the sources.
- **2026-05-29** — `oglasino-docs` Φ4 (`expo-service-error-contract`) close-out: added the feature-close `decisions.md` entry at top (mobile service layer + error contract shipped — `parseServiceError` plumbing + ~14 services surface-via-throw, `reportedReviewId` review-report wire, `@react-native-community/netinfo` Gate 0 offline gate; carry-forwards for chat A and the Ω chat recorded in the entry tail). `state.md`: new Φ4 active-features block (`shipped (code)` / `verifying`); Expo structural foundation umbrella flipped to `shipped (code)` with an all-four-shipped Tasks-remaining; cleared the two stale "Φ2 opens next" / "Next up: Φ2" lines (now chat A); closed the "service layer silently swallows" Risk Watch row (structural audit F18); added `@react-native-community/netinfo` to the pending iOS+Android-rebuild Risk Watch row; struck mobile-adoption from Review Reports' Tasks-remaining and flipped its Expo-backlog row to `adopted` via `oglasino-expo-service-error-contract-3`. `issues.md`: no change (the 2026-05-29 maintenance-redundancy entry left untouched, per the brief). Flipped `features/expo-service-error-contract.md` status `planned` → `shipped (code)` / `verifying` and added a Session log. Archived **five** summaries from `oglasino-expo/.agent/` to `sessions/` — the Phase-2 audit session summary (`-1`), the three code-brief summaries (`-2` plumbing, `-3` review-wire, `-4` offline), and the audit deliverable `audit-expo-service-error-contract.md` — and deleted the sources. **Brief-vs-reality:** the brief labeled `-1` as "brief 1" and listed three summaries + audit; in reality `-1` is the audit session summary and the three code briefs are `-2`/`-3`/`-4`, so five files were archived (matching the established "audit `-1` + impl `-2…`" precedent) and the chat-A caller-list pointer was corrected from `-1` to `-2` (the error-plumbing summary).
- **2026-05-29** — `oglasino-docs` `version-checksums-per-language` close-out: added the feature-close `decisions.md` entry at top (translation checksums on `GET /api/public/versions` now per-(namespace, language); per-NS-collapsed transitional contract replaced; posture B / four-language / `/v2`-rejected rationale; `future/version-checksums-per-language.md` roadmap pointer closed). Added a **new** Expo backlog row `Version checksums (per language)` → `features/version-checksums-per-language.md` (`shipped` / `mobile-stable`, adopted in `oglasino-expo-version-checksums-per-language-2`; `-1` is the read-only Phase 2 audit) and left the predecessor `Version checksums` row (the boot-redesign per-NS-collapsed adoption) untouched per Igor. `Last updated` header was already `2026-05-29` (the stale-date the brief expected was already corrected by the prior session) — no change. Archived six summaries from `oglasino-backend/.agent/` and `oglasino-expo/.agent/` to `sessions/` (backend audit `-1` + impl `-2`, expo audit `-1` + impl `-2`, plus the two Phase 2 `audit-version-checksums-per-language.md` deliverables, repo-disambiguated per the boot-redesign precedent) and deleted the sources. Grep confirms the only live `future/...` references are the two intentional hits (feature-spec "supersedes" codespan + the archived boot-redesign session).
- **2026-05-29** — `oglasino-docs` `system-error-code-split` + `review-reports` close-out: applied two `decisions.md` entries (review-reports above the error-code split, both newest-on-top); added two `web-stable` active-feature blocks (Review Reports, Error Code Domain Split) + one Expo backlog row (Review Reports; none for the split — no mobile-adoptable surface) + one Risk Watch row (two new `report.reported_review*` placeholders); amended `features/review-reports.md` §6.3 + §14 for the null-safe self-report and flipped its status to `web-stable` (matched on `features/system-error-code-split.md`); corrected the 2026-05-28 `issues.md` REVIEW-wire entry runtime description and flipped it to `fixed` (no new issues entry). Archived eight engineer summaries from `oglasino-backend/.agent/` and `oglasino-web/.agent/` to `sessions/` (backend: system-error-code-split-1, system-error-code-1 audit, review-reports-1, audit-system-error-code; web: system-error-code-split-1, system-error-code-1 audit, review-reports-1, audit-system-error-code) and deleted the sources. One file held back unarchived: `2026-05-29-oglasino-web-report-406-error-shape-1.md` is corrupted (4660 lines, runaway duplication, missing mandatory Part 5 sections) — flagged to Igor for the web engineer to regenerate.
- **2026-05-28** — Bug chat: product view-count surface. Two defects fixed and stage-confirmed — view-count increment never firing (`cookies()`-inside-`after()` web throw + `LANG_MISSING_OR_INVALID` backend 400 + `Long::getLong` owner-cache fold-in) and view-count re-counting on every refresh (token-bucket-as-deduper replaced with SETNX per-viewer-per-12h-window + CF-Connecting-IP keying). Cleanup folded in: dead rate-limiter machinery deleted, two mis-named TTL config keys renamed to the `redis.product.view.*` convention. Two `decisions.md` entries authored (skipAuth ban reversal; view-dedup mechanism). No new `issues.md` entries (per Igor); increment bug flipped to `fixed`, Risk Watch row closed. Nine session summaries archived across `oglasino-backend` and `oglasino-web`.
- **2026-05-28** — `oglasino-docs` Φ3 close-out: 22 engineer session summaries (Briefs 1–19a) archived from `oglasino-expo/.agent/` to `oglasino-docs/sessions/` and source files deleted. Three new `decisions.md` entries applied (Φ3 scope expansion, barrier-review lesson, Φ3 boot-loading UNRESOLVED handoff). New Φ3 entry in `state.md` Active features marked `verifying` (code-complete, smoke-FAILING on unresolved cold-start boot-loading issue). New Risk Watch row for the cold-start hang. Five new `issues.md` entries (cold-start boot-loading hang; live `[BOOT]` diagnostic instrumentation in `api.ts`; chat-store require-cycle triangle Cycle B; always-mounted `<Stack>` exposes pre-bootstrap API calls — mitigated-not-resolved; pre-existing low-severity carry-overs confirmed). F10 `AppContext.Provider` value not memoized flipped to `fixed`. Handoff brief at `.agent/handoffs/phi3-bootloading.md` for the resolving Mastermind chat.
- **2026-05-28** — Bug-chat close-out: seven bugs flipped to `fixed` across `oglasino-web` and `oglasino-backend` (#5 two-redirect via `proxy.ts` cookie-hoist, #16 malformed-429 via 429-scoped synth normalizer, #8 `isFollowingCurrent` via Fix A dropping `skipAuth` on `getUserForId` + portal `getProductDetails` + `markFollowUser` cache-bust, #2 circular-dep documentation note, #13 `baseSite` audit verdict clean, report-submit backend half via new `ReportErrorCode` family + per-target dedupe + existence/self-report/deletion guards, view-count display half via `< 0` + `hasFetched` + `null`-on-failure in `NumberOfViews`/`getNumberOfViews`). One bug parked-OPEN: 2026-05-28 view-count `/seen` increment never fires (`after()` fix failed manual smoke; backend confirmed clean; prime suspect is post-#8 `owner.iamActive` gate). One follow-up pending: web mapping of new `ReportErrorCode` family. Three `decisions.md` entries applied (429-scoped synth, report trust-boundary closed, #8 Fix A). Two Risk Watch rows added (view-count increment; report.* native translator review). 12 session summaries archived from `oglasino-web/.agent/` and `oglasino-backend/.agent/` to `oglasino-docs/sessions/`.
- **2026-05-27** — Φ2 close-out chat: navigator-remount loop diagnosed and fixed (`_layout.tsx` always-mount Stack + overlay pattern, `(portal)/_layout.tsx` `selectedBaseSite` chrome guard, `AppContext.tsx` effect-dep fix). Two reverted detours captured in decisions.md 2026-05-27.
- **2026-05-27** — `oglasino-web` theme-cookie-persistence sessions 2 and 3: Brief 1 (single-shot) replaced `next-themes` with cookie-based theme system — new `useThemeStore` Zustand store at `src/lib/store/useTheme.ts`, SSR pre-paint snippet helper at `src/lib/theme/ssr.ts`, `SyncThemeFromCookie` + `SyncThemeFromSystem` mounted in `AppInit.tsx`, tri-state segmented toggle (Sun/Monitor/Moon) in `PortalConfigDialog`, three consumer migrations (`OglasinoIcon`, `sonner`, `ToggleButton`), `GlobalCookie.theme` widened to `'light' | 'dark' | 'system'`, `next-themes` dependency removed, `ThemeProvider.tsx` deleted. Brief 1b (hydration fix) added server-side `.dark` class rendering on `<html>` for cookie-concrete branches and restored `suppressHydrationWarning` for the `'system'` / absent-cookie branches where server can't resolve `prefers-color-scheme`. Closes audit observations 2 (`OglasinoIcon` deps) and 5 (`ToggleButton` mounted-guard) in passing. 229 web tests passing throughout.
- **2026-05-26** — `oglasino-expo` Expo cloud setup completed: new cloud project `@oglasino/oglasino-expo` (ID `382cac59-45fa-420e-ab80-7fc709e6d2f3`) created to replace deleted broken project, three-tier build profile structure landed (`development` / `preview` / `production` with per-tier bundle IDs `com.oglasino.development` / `com.oglasino.preview` / `com.oglasino`), four new Firebase Apps registered in `oglasino-stage-49abb` (Android + iOS for both development and preview tiers), `app.config.ts` rewritten with three-way `APP_ENV` branching across bundle ID, package, googleServicesFile, name, scheme, `eas.json` rewritten with three profiles, `.env.preview` created with stage Firebase + stage backend (`https://api-stage.oglasino.com/api`) values, `.eas/workflows/preview.yml` and `production.yml` written for auto-build triggers, `.gitignore` switched to wildcard `.env.*` + `!.env.example`, six tier-specific Google service files placed (`*.development.*` / `*.preview.*` / `*.prod.*`), three Apple Developer App IDs registered (`com.oglasino` / `com.oglasino.development` / `com.oglasino.preview`) with Push Notifications capability, first development build succeeded on EAS Cloud for both iOS and Android, iOS install + Metro connection + Firebase email/password login end-to-end validated on physical iPhone, Android email/password login validated, SHA-1 and SHA-256 fingerprints extracted from EAS-managed Android development keystore and registered against `com.oglasino.development` in stage Firebase project, keystore backed up to `oglasino-private/01-secrets/oglasino-infra/oglasino-expo-android-keystore.jks`, `firebaseClient.ts` hardened with missing-env-var guard and dead `testConnection()` removed, per-tier `name` and `scheme` added to `app.config.ts` (`Oglasino Dev` / `Oglasino Preview` / `Oglasino` and `oglasino-dev://` / `oglasino-preview://` / `oglasino://`), legacy `GoogleService-Info.dev.plist` and `google-services.dev.json` deleted, doc drift in `IMAGE-PIPELINE-RN-AUDIT.md` updated from `com.oglasino.dev` to `com.oglasino.development`, two inert legacy Firebase Apps (`oglasino-android-stage` and `oglasino-ios-stage`) deleted from `oglasino-stage-49abb`. New spec at `infra/expo/cloud-setup.md` and pre-deploy checklist at `infra/expo/pre-deploy-checklist.md`. Final Android + iOS rebuild deferred to capture all post-first-build changes; Google Sign-In validation deferred to post-rebuild.
- **2026-05-25** — `oglasino-docs` image-alt-translation close-out (session 3): flipped feature status to `shipped` in `state.md` and `features/image-alt-translation.md`. Closing `decisions.md` entry summarizing four keys × four locales = 16 seed rows, five code sites swapped, spec amended mid-feature for C2 description fold-in. Four `issues.md` entries from the three audit summaries (redundant per-tenant translation cache, about page content duplication, hardcoded `rs-sr` SearchAction, intro page title + copyright `wontfix`). Six engineer session summaries archived from `oglasino-backend/.agent/` and `oglasino-web/.agent/` to `oglasino-docs/sessions/`. Feature spec line numbers updated to match as-shipped state. Risk Watch row added for native-translator review. Expo backlog row added (web-only metadata; mobile equivalent is separate work).
- **2026-05-25** — `oglasino-expo` Φ1 catalog dead-code cleanup: deleted three unreachable files forming a self-contained dead-code cluster — `src/lib/store/useCatalogStore.ts` (Zustand store, zero callers; was an empty file on disk via uncommitted modification), `src/lib/hooks/useCatalog.ts` (React hook, zero callers), `src/lib/storage/catalogStorage.ts` (AsyncStorage wrapper, only called by the dead hook). Live catalog data path in `AppContext.tsx` untouched. `CatalogDTO` and other live types preserved. Audit prompted by Igor noticing the empty `useCatalogStore.ts` file; the audit revealed the cluster had been dead since before Φ1 opened — Brief 2 / 2-bis briefly added and removed an import from `authStore.ts` but no other code path ever referenced these files. 109 tests passing post-cleanup.
- **2026-05-25** — `oglasino-expo` Φ1 Brief 7 (F5 — 401/403 axios response interceptor): extended `api.ts` response interceptor with 403 USER_BANNED/EMAIL_BANNED handler (sign out, set `accountBanned: true`, never-resolving promise) and 401 token-refresh-and-retry with single-flight queueing (concurrent 401s queue, only one `getIdToken(true)` runs at a time, sign out on second 401). Existing handlers preserved. `_retry` flag via TypeScript module augmentation. 109 tests passing. Closes F5 and Φ1.
- **2026-05-25** — `oglasino-backend` Q2 verification (read-only): confirmed all three 403 paths (`FirebaseAuthFilter` mid-session, `firebase-sync` disabled user, `firebase-sync` banned email) produce `{errors: [{field: null, code: <CODE>, translationKey: <KEY>}]}`. Codes: `USER_BANNED` (mid-session and sign-in-disabled) and `EMAIL_BANNED` (sign-in-banned-hash). Spec drift found: spec at `user-deletion.md` §8.8 documents `translationKey: errors.user.banned` / `errors.email.banned` but code emits `user.banned` / `email.banned`. Spec correction in Docs/QA closing batch.
- **2026-05-25** — `oglasino-expo` Φ1 Brief 6 (F4 — foreground re-validation): new `ForegroundRevalidationInit.tsx` (Option A — separate init component per established codebase pattern). `AppState` listener tracks `backgroundedAt` via `useRef`. On `active` after >5 min, calls `syncUserToBackend`. Threshold constant: `FOREGROUND_REVALIDATION_THRESHOLD_MS = 5 * 60 * 1000`. Response handling delegates to Brief 4A's plumbing.
- **2026-05-24** — `oglasino-expo` Φ1 Brief 5 (F13 — auth guards): three layout files guarded with `useAuthStore(s => s.user)` + `_hasHydrated` reads, `<Redirect href="/" />` for unauthenticated users post-hydration, `null` while not hydrated. No `SessionGuard`-equivalent existed; guards inlined per Part 4a. `DashboardLayout` → `OwnerLayout` rename in `app/owner/_layout.tsx` (cosmetic disambiguation).
- **2026-05-24** — `oglasino-expo` Φ1 Brief 4B (F1 part 2 — `AccountStateDialogsInit`): new component subscribes to `restored` and `accountBanned` flags, opens `InfoDialog` via existing `DialogManager`. `accountJustDeleted` subscription stub in place for chat E. `closeButtonLabel?: string` prop added to `InfoDialog` (backward-compatible). Translation keys reuse confirmed: `common.system.account.restored.*`, `banned.dialog.*`, `buttons.banned.go.home.label`.
- **2026-05-24** — `oglasino-expo` Φ1 Brief 4A (F1 part 1 — `initAuthListener` wiring): listener wired from `AppInit`, hydration-gated. Module-scoped single-flight guard (`inFlightUid` + `lastSyncedUid` + 2-second `lastSyncedAt`) mirrors web's `UseTokenRefresh` pattern. Switched from `onAuthStateChanged` to `onIdTokenChanged` direct usage. `restored` and `accountBanned` flags + setters added to `useAuthStore`. `X-Account-Restored` header reading added to axios response interceptor in `api.ts` (interceptor-layer placement, matches web). New `isErrorWithCode` utility in `src/lib/utils/`.
- **2026-05-24** — `oglasino-expo` Φ1 Brief 3 (F22 — hydration race): `ChatsInit` and `NotificationsInit` gated their effect bodies on `_hasHydrated` via separate selector subscriptions (no `useShallow` — Φ3 work). `InitFavoritesStore` correctly identified as Option B (Firebase `onAuthStateChanged` directly, not Zustand state — race doesn't apply); no change.
- **2026-05-24** — `oglasino-expo` Φ1 Brief 2-bis (F3 correction): removed `useCatalogStore.getState().clearCatalog()` call from `authStore.logout()` plus the now-unused `useCatalogStore` import. Catalog is not user-scoped (identical for every user on the same baseSite); does not belong in logout cleanup. Catalog freshness will be handled by future version-based invalidation (new backlog entry — Catalog versioning).
- **2026-05-24** — `oglasino-expo` Φ1 Brief 2 (F3 — direct store cleanup on logout): added direct clear calls for six user-scoped stores in `authStore.logout()`, each wrapped in its own try/catch with `logServiceError`. Replaced pre-existing `console.error('Logout failed', err)` with `logServiceError`. `userPreferenceStorage` skipped — confirmed zero consumers across the codebase (dead code, separate Ω cleanup target).
- **2026-05-24** — `oglasino-expo` Φ1 Brief 1 (F2 — `logoutFirebase` calls `auth.signOut()`): two-line addition. Firebase signOut now fires before `GoogleSignin.signOut()` so the `onIdTokenChanged(null)` cascade triggers downstream cleanup. Wrapped in try/catch with `logServiceError` so network-less logout doesn't block the rest of the sequence.
- **2026-05-24** — Mastermind Φ1 chat opened. Spec `features/expo-auth-lifecycle.md` authored covering all seven findings (F1, F2, F3, F4, F5, F13, F22) plus the cross-repo Q2 verification gate. Backend-constraint posture locked: no backend changes in Φ1, mobile conforms to existing contracts. Engineer brief sequencing established: F2 → F3 → F22 → F1 (split into 4A/4B) → F13 → F4 → F5 (blocked on Q2).
- **2026-05-24** — Mastermind, Expo structural audit closeout: read-only audit of `oglasino-expo` across seven structural categories produced 26 findings (F1–F26) plus 18 out-of-scope observations and 3 cross-repo questions. Rating 5.5/10. Verdict: insert four foundation chats (Φ1 auth lifecycle, Φ2 navigation, Φ3 performance, Φ4 service layer + error contract) before chat A; rescope A–I (most chats become smaller); append Ω (final structural sweep) and Ψ (runtime verification) at the end. New spec at `features/expo-structural-foundation.md`. Handoff brief for the Φ1 Mastermind chat drafted. Audit files archived from `oglasino-expo/.agent/` to `oglasino-docs/sessions/`.
- **2026-05-24** — `oglasino-docs` chat-α closeout: applied drafted `decisions.md` entry for mobile admin removal; updated `features/expo-release-readiness.md` section 5 row α with shipped status; added Expo release readiness to active features in `state.md`; archived `2026-05-24-oglasino-expo-admin-removal-1.md` from `oglasino-expo/.agent/` to `oglasino-docs/sessions/` and deleted the source; archived `audit-expo-readiness-admin-removal.md` from `oglasino-expo/.agent/` to `oglasino-docs/sessions/` and deleted the source.
- **2026-05-23** — Igor: GA4 admin runbook executed for the prod property (`oglasino-prod-7e5db`, `G-GNKB4WBNC0`). Stream URL set to `https://oglasino.com` (was blank when Firebase auto-created the property). 19-20 event-scoped custom dimensions registered. Internal-traffic filter active with Igor's home IP. 14-month retention with reset-on-new-activity. Reporting Identity "Blended" (default, enables Consent Mode v2 behavioral modeling). Google Signals left OFF. Two items deferred to post-launch: conversion-event marking (requires events to exist first), Search Console verification + GA4 link (Search Console property `oglasino.com` not yet verified — natural trigger is 1-2 days before prod deploy).
- **2026-05-23** — Igor: GA4 admin runbook executed for the stage property (`oglasino-stage-49abb`, `G-P0LEVEJ0V9`). 19-20 event-scoped custom dimensions registered, four events marked as conversions (`sign_up`, `product_create_completed`, `contact_seller_clicked`, `message_sent`), internal-traffic filter active with Igor's home IP, 14-month retention with reset-on-new-activity. Stage property production-ready for stage traffic.
- **2026-05-23** — `oglasino-web` GA4 v1 brief 6 (engagement events). `contact_seller_clicked` wired into `CallUserButton.tsx`, `StartMessageButton.tsx`, `FavoriteButton.tsx` (add-transition only); `message_sent` wired into `useChatStore.sendMessage` post-`batch.commit()`. `CallUserButton` gained a required `productId` prop, plumbed from `ProductFunctions.tsx` (the single call site — audit named `UserDetails.tsx` which was found incorrect). All five firing sites confirmed PII-free. 229 tests passing, 175 lint warnings baseline. Closes the code-brief sequence for the feature.
- **2026-05-23** — `oglasino-web` GA4 v1 brief 9 (form-failure events). `parseProductValidationErrors` refactored to return `{ byField, list }` dual-view shape preserving Part-7 `code` end-to-end (audit's framing of the current return shape was wrong — three component consumers would have required ~30 mechanical edits to switch to per-error iteration). Backend-validated forms (product create step 1 + step 4, product update) fire with real Part-7 codes; client-Zod forms (register, login, profile update) fire with synthetic codes defined inline. `SuggestCategoryDialog` left unchanged (boolean error shape too coarse). 229 tests passing.
- **2026-05-23** — `oglasino-web` GA4 v1 brief 8 (error events). `app/error.tsx` default-export renamed `GlobalError` → `RouteError`, `console.error` swapped for `trackError(error, { boundary: 'route' })`. `app/global-error.tsx` same swap with `boundary: 'global'`, function name unchanged. New `GA4ErrorListener` initializer mounted from `AppInit.tsx` registers `window.addEventListener('error', ...)` and `window.addEventListener('unhandledrejection', ...)` with `instanceof Error` normalization. Closes `issues.md` 2026-05-21 `GlobalError`-naming entry.
- **2026-05-23** — `oglasino-web` GA4 v1 brief 7 (search and filter events). `search` fires in `SearchInput.commitSearch` with 100-char truncation; new `ViewSearchResultsTracker` client island mounted from home and catalog pages fires `view_search_results` once-per-mount when a search query is present; `filter_change` fires in `FilterManager.tsx` SYNC-TO-URL effect, gated by `hydrated` + `newUrl !== current` so URL hydration doesn't trigger spurious events. 229 tests passing.
- **2026-05-23** — `oglasino-web` GA4 v1 brief 5 (product events). New `ProductViewTracker` client island mounted from product detail page fires `product_view` once per mount with `is_owner_view` computed from `useAuthStore.user.id === ownerId`. `product_create_started` fires from `CreateNewProductDialog.tsx` mount effect when the wizard's first user-facing step (`currentStep === 0`) mounts. `product_create_completed` fires as the first statement inside `UploadedProductDialog.tsx` success branch so a downstream throw or navigation cannot prevent the event landing. 229 tests passing.
- **2026-05-22** — `oglasino-web` GA4 v1 brief 4 (auth events). `AuthUserDTO` TypeScript shape gains `wasRegister: boolean` mirroring brief 3's backend response. `syncUserToBackend` returns `{ user, wasRegister }`. `UseTokenRefresh.tsx` consumes the destructured result inside its single-flight guard and fires `track('sign_up' | 'login', { method, user_id })` after `setUser`, before `initPushForAuthenticatedUser`. Provider mapping: `password → email`, `google.com → google`, `facebook.com → facebook`. Six new tests in `authService.test.ts`; 229 tests passing.
- **2026-05-22** — `oglasino-backend` GA4 v1 brief 3 (backend `wasRegister`). New `SyncResult(User user, boolean wasRegister)` record returned by `FirebaseAuthService.getOrCreateUser`. `wasRegister` derived inside `createUserSynchronized` at the `userService.saveUser(newUser)` `INSERT` branch — concurrent-create race correctly resolves with loser thread reporting `wasRegister=false`. `AuthUserDTO` gains the field; `AuthController.firebaseSync` populates from `SyncResult`. Seven `AuthControllerFirebaseSyncTest` tests including two new branches and one `$.wasRegister.doesNotExist()` assertion on the USER_BANNED error path. 551 backend tests passing.
- **2026-05-22** — `oglasino-web` GA4 v1 brief 2 (script load + stub mounting + page_view). `app/layout.tsx` adds two `<Script strategy="afterInteractive">` blocks gated on `NEXT_PUBLIC_GA4_MEASUREMENT_ID`. `app/[locale]/layout.tsx` mounts `<GA4RouteListener />` as a sibling to `<NavigationProgressBar />`. `AppInit.tsx` mounts `<GA4UserIdSync />` between `<UseTokenRefresh />` and `<AccountStateDialogsInit />`. First user-visible behavior: `page_view` fires on pathname change after script load.
- **2026-05-22** — `oglasino-web` GA4 v1 brief 1 (foundation). `src/lib/analytics/track.ts` (three guards, debug-mode injection from `NEXT_PUBLIC_GA4_DEBUG_MODE`); `src/lib/analytics/trackError.ts` (Error normalization, 200-char truncation); `GA4RouteListener.tsx` and `GA4UserIdSync.tsx` stub initializers (not mounted yet — brief 2 mounts them). `.env.local.example` updated with GA4 entries. 12 new unit tests across `track` and `trackError`.
- **2026-05-21** — `oglasino-web` portal cold-load cascade Brief 3 (UseTokenRefresh single-flight): `useRef`-held single-flight guard on the `onIdTokenChanged` cascade with a 2-second same-uid drop window. `writeFirebaseTokenCookie(token)` hoisted into the listener so the cookie mirror runs on every emission. Guard resets on sign-out. `lastSyncedAt` set only on success. Lint/typecheck/test (206 passing) green. Manual runtime verification (hard-refresh count drop, normal-refresh non-regression, sign-out → sign-in re-fire, cookie-mirror inspection) owed by Igor before final closure.
- **2026-05-21** — `oglasino-web` portal cold-load cascade Brief 2 (FilteredProductList random-seed key): destructure `randomSeed` out before stringifying `filtersData` for the React key; seed still travels to backend via unmodified `filtersData` on the request body. Lint/typecheck/test green.
- **2026-05-21** — `oglasino-web` portal cold-load cascade Brief 1 (FilterManager refresh fix): Lever A normalized the trailing-slash false-positive on the `pathname === '/'` URL construction; Lever B deleted the redundant `setTimeout(() => router.refresh(), 0)` after `router.replace`. Hard refresh of `/rs-sr`: 4 → 2 `POST /api/public/product/search`. Normal refresh: 3 → 1. Other FilterManager-mounted routes (`/owner/products`, `/admin/products`, `/admin/products/[userId]`, `/catalog/...`) scanned and confirmed not regressed. Lint/typecheck/test (206 passing) green.
- **2026-05-21** — `oglasino-web` portal cold-load cascade Audit 2 (instrumentation): two temporary `console.log` lines added and removed in the same session; reconciled the two post-mount RSC re-renders to a single FilterManager SYNC TO URL effect run firing `router.replace` + `setTimeout(router.refresh)`. Audit's prior hypothesis #1 ("effect fires twice") refuted; observation #5 ("`router.replace + setTimeout(0) → router.refresh` double-fetches in Next 16") confirmed as the actual mechanism. Empty `last-session.md` from this session's twin was a known gap; resolved naturally when the next session's twin overwrote it.
- **2026-05-21** — `oglasino-web` portal cold-load cascade Audit 1 (read-only): mapped every code path triggering `POST /api/public/product/search` on the portal home. Found two SSR call sites (page-render + `generateMetadata` snapshot) and ruled out every client-side site (favorites refetch is `/favorites`-gated, pagination short-circuits on page 0). Identified `randomSeed` × `JSON.stringify(filtersData)` key as the "jerk" mechanism. Identified `onIdTokenChanged` listener as the sole caller of `syncUserToBackend` (sole-hydrator contract intact). Could not deterministically identify trigger of the two post-mount RSC re-renders from code alone; gap closed by Audit 2.
- **2026-05-20** — `oglasino-docs` session 1 of messaging close-out (Brief 6b): three `issues.md` entry status flips (2026-05-17 `tempProductReason` clearing → fixed; 2026-05-14 chat list capped at 15 → fixed; 2026-05-14 silent text-only send failure → fixed); two new `issues.md` entries (Brief 5 cleanup cron smoke deferral; `TestCreateJSON.java` anonymous public endpoint); closing `decisions.md` entry summarizing the messaging feature; this set of `state.md` edits; small spec amendments to `features/messaging.md`.
- **2026-05-20** — `oglasino-backend` + `oglasino-web` session of messaging Brief 6a: backend seeded `MESSAGES_PAGE.chats.load.more` in all four locales (next-available IDs 3363/EN, 5463/RS, 1263/CNR, 7563/RU; RU value matches the existing Latin-transliterated convention); web swapped the placeholder `messages.load.more` to `chats.load.more` in `Chats.tsx`. 200 web tests, 548 backend tests passing.
- **2026-05-20** — `oglasino-backend` messaging Brief 5 (cleanup cron): new `pending_chat_cleanup` and `messaging_cleanup_locks` tables in V1; `DefaultMessagingCleanupService` with `@Scheduled` cron; cross-feature touch into `DefaultUserDeletionService.runHardDelete` (one repository insert). 11 cron + integration tests passing; sentinel format `"deleted:<original-uid>"`. First-production-Sunday smoke deferred per Igor's choice; logged in [issues.md](issues.md).
- **2026-05-20** — `oglasino-web` messaging Brief 3 (auto-linkify): `linkify-it@5.0.0` added (5× smaller unpacked than `linkifyjs`, MIT, narrowed scheme allowlist excludes `javascript:`/`data:`/`vbscript:`). 12 tokenizer tests. Anchor visible-text is the user's raw URL substring; `href` is linkify-normalized. 166 web tests passing.
- **2026-05-20** — `oglasino-backend` messaging Brief 4: admin chat-list receiver bug fixed in `DefaultFirebaseChatService.getReceiverFromChatDoc` (was reading first doc's `users` for every row → now reads per-doc); deleted public `/api/public/notification/test` endpoint (NotificationsControllerTest + NotificationTestDTO); seeded `messages.send.failed.toast` + `messages.image.preview.fallback.label` in all four locales; confirmed `NotificationCategoryId.MESSAGE` has zero callers (intentional stub). 539 backend tests passing.
- **2026-05-20** — `oglasino-web` messaging Brief 2 (web core): 11 sub-edits — `tempProductReason` → `tempProductContext` rename + clearing fix + atomic `writeBatch` on both new-chat and existing-chat paths + send-failure toast lifted to React boundary + temp state cleared on navigation away from `/messages` + `getActiveChat` Firestore fallback fix + chat list "Load more" button + `deleteChat` reorder + mark-seen failure handling + deleted-user fallback rendering + polish bundle (F17a/b/c/d). 154 web tests passing.
- **2026-05-20** — `oglasino-firestore-rules` messaging Brief 1b: `get('productId', null)` immutability fix in the message update rule; new test 24 for the absent-`productId` case. 24 rule tests passing.
- **2026-05-20** — `oglasino-firestore-rules` messaging Brief 1: six rule changes (users owner-only, chats update freezes users[] and delete `if false`, messages `get` → `getAfter` on read/create/update, receiver-only `seen` flip, field-shape immutability, userchats read/delete owner-only, notifications create `if false`). 23 tests seeded; `--passWithNoTests` removed; CI workflow runs tests before deploy dry-run. Smoke-verified on stage (`oglasino-stage-49abb`): new-chat send batch lands cleanly, existing-chat sends work, block/unblock work.
- **2026-05-20** — Mastermind, google-analytics-v1 + consent-mode-v2 planning: Phase 1-4 complete. Two parallel web audits (`ga4-discovery`, `consent-audit`) ran read-only in `oglasino-web`; seam analysis surfaced four blocking consent gaps, splitting the work into two features. Ten Mastermind decisions locked (see `decisions.md` 2026-05-20 entry). Canonical specs authored at `features/consent-mode-v2.md` and `features/google-analytics-v1.md`. Chat closes; Phase 5 opens in a fresh Mastermind chat with consent foundation brief 1.
- **2026-05-20** — bug chat closeout: easy-tier batch (6 bugs across `oglasino-web`, `oglasino-router`, `oglasino-backend`) plus one `issues.md` entry resolved by Igor directly. Closed: `/owner/follows UserCard` now removes the unfollowed row from the list (parent-callback wiring, mirrors `loadMore` state pattern); `oglasino-router` worker's `use.backend.check` KV read hoisted into the fail-open `Promise.all` alongside the other two flags, and the top-of-file matrix-comment extended with the third input (two `issues.md` entries closed in one session); `GlobalIndexerService.printStackTrace()` swapped for project-standard SLF4J `log.error` at ERROR level with indexer-name context; three pre-existing `-Xlint` warnings fixed after explicit pre/post-upgrade verification (`AppVersionController` semver4j API swap, `ProductIndexer` raw-Map tightened to `TypeReference<Map<String, Object>>()`, `GlobalExceptionHandlerTest` narrow-scope deprecation suppression pending the coordinated `UNPROCESSABLE_ENTITY → UNPROCESSABLE_CONTENT` sweep); `opentelemetry-semconv` pin disposition verified by Igor and closed. One additional in-chat adjacent fix not corresponding to a tracked `issues.md` entry: `FollowUserButton` 2-second `setBlocked` debounce now releases synchronously on the failure branch (success branch debounce unchanged). Web lint pass 1 (mechanical, harmless-only) executed against the 2026-05-16 entry: baseline 207 warnings → after 185 = 22 fixed (all `: any` annotations on call sites with mechanically inferable types). Pass 1 surfaced one type-safety bug (`UserFilters.tsx` `'role'` comparison against a type with no `role` key, fixed by Igor directly). The 2026-05-16 lint entry remains open by design — pass 2 (translation typing helper, `images.remotePatterns` decision, state-shape tightening, `react-hooks/*` rules) deliberately deferred outside this chat per categorized inventory in the engineer session summary. Net `issues.md` change: 7 entries flipped to `fixed`. Backend test suite at 502 passing post-batch (matches Bug #8 baseline); web test suite at 154 passing.
- **2026-05-20** — bug chat closeout: very-easy batch (9 bugs). Nine backend and web sessions across `oglasino-web` and `oglasino-backend` plus two fixes by Igor directly. Closed: `/owner/follows UserCard` toast contract widened to discriminated-union, both consumers migrated, failure-path debounce released synchronously (3 sessions); `Kategorije iz` breadcrumb literal localized via new `NAVIGATION` key in 4 locales; `markdown.fild.load` typo renamed (Igor); pricing-hero subtitle duplicate fixed (Igor); global header search Enter-key handler added (uniform across portal/owner/admin); `NewProductRequestDTO.free` dead field removed (closes a decorative trust-boundary surface); `auditRequiredConfig` now fails boot on every environment instead of logging ERROR; six legacy unsuffixed regex/spam rows deleted from `data-configuration.sql`; two `COMMON`-namespace tooltips on the product detail page localized in 4 locales. Net `issues.md` change: 9 entries flipped to `fixed`. Backend test suite at 502 passing post-batch; web test suite at 154 passing.
- **2026-05-18** — `oglasino-docs`, qa-preparation session 19: final docs cleanup brief — content fixes (home-page top-level filter clarity, free-zone two image descriptions, no stale-defect cuts found), `issues.md` line-number backfill on two 2026-05-14 catalog-slug entries plus dependency-note cross-references, Known-issue cross-reference promotions across 6 pitfalls in 5 topics (`catalog-page` slug, `product-page` Serbian tooltips, `category-navigation` Kategorije label, `follow-flow` isFollowingCurrent, `messages-page` silent-send + 15-cap), flow-topic `route` field standardization (3 topics, primary-surface-only), image-comment retrofit final audit (verified all 58 entries across 17 topics, 0 additions). Closing `decisions.md` entry drafted and applied (10 sections summarising sessions 8–19). Conventions Part 5 amended: archive-pointer stub rule (7A) + Config-file impact section formalised in the engineer template (7B). `npx tsc --noEmit` from `oglasino-web` clean.
- **2026-05-17** — Mastermind, configuration-audit close-out: cross-file audit of every CLAUDE.md, bootstrap, and the four config files surfaced 17 findings. Resolved structurally rather than as a typo sweep: Docs/QA becomes sole writer of `conventions.md`, `decisions.md`, `state.md`, `issues.md`, with session-closure gate preventing pending-draft closure (Entry 1 in `decisions.md`). Router agent acknowledged as fifth engineer agent across conventions Parts 2/3/4/8/9 and the five CLAUDE.md files (Entry 2). Mobile catch-up session slug discipline pinned to per-feature, not per time window; Expo backlog table added to `state.md` (Entry 3). Conventions Part 11 heading fixed, Part 10 reformatted to ATX headings, Part 5 `<n>` indexing disambiguated, "Config-file impact" section added to engineer session template. Four bootstraps rewritten to route config writes through Docs/QA and add closure gates. Five CLAUDE.md files updated: engineer count flipped to five, "no writes to the four config files" hard rule added, session-summary references updated, Docs/QA CLAUDE.md rewritten with new write authority. Three issues.md entries from the audit logged: `master-plan.md` content outdated (this session), plus the configuration-audit findings absorbed into the conventions rewrite itself.
- **2026-05-17** — `oglasino-backend`, bug chat closeout: six sessions on the connection-pool incident's deferred TTL question and three adjacent bugs surfaced during investigation. Two read-only investigations (`redisBaseSites`, then the other six caches plus translations) established that five reference caches have no runtime write path, and four code sessions implemented the verdicts: (1) TTL removed from `redisBaseSites`, `redisBaseSiteOverviews`, `redisLanguage`, `redisLanguages`, `redisBaseCurrency`; TTL kept on `redisUserInfo` / `redisUserAuth` with documenting comments; helper signature reformed `long ttl` → `Duration ttl` (`null` = no expiry). (2) `DefaultTranslationService.updateTranslation` now refreshes the in-memory `backendTranslations` map on `BACKEND_TRANSLATIONS` edits — closes the admin-edit-not-visible-until-restart gap on push notifications and email content. (3) `DefaultUserFacade.updateCurrentUserData` evicts `redisUserInfo` after `generateUserTranslations` — removes the ordering-dependent `shortBio` freshness coupling the deferred-follow-up Javadoc warned about; Javadoc deleted. (4) `redisBaseCurrency` cache value swapped from `Currency` entity to `CurrencyDTO` — closes the latent lazy-init footgun if `Currency` ever grows a `@ManyToOne`. 360 tests passing at the end of the fourth code session (baseline 355 → +2 + 1 + 2). Closes the daily-trigger window from the 2026-05-16 production incident; structural concurrent-cache-miss fix remains in [features/connection-pool-hardening.md](features/connection-pool-hardening.md).
- **2026-05-17** — `oglasino-docs`, qa-preparation session 11: `category-navigation` topic appended (`type: 'flow'`, entry 15) — first flow topic in the feature. Covers header catalog menu + breadcrumb chain + slug routing as three surfaces, with four entry points (header menu, breadcrumbs, subcategory selector dialog, direct URL). All five content arrays used; structural-choice rationale for flow topics captured in the session "For Mastermind." Five pitfalls (one "Known issue" pitfall cross-referencing the two 2026-05-14 catalog-slug `issues.md` entries), 14 checklist items, three images. `relatedTopics`: `catalog-page`, `product-page`, `home-page`, `global-header-search`. Typecheck clean. One new `issues.md` entry — hardcoded Serbian "Kategorije iz \<X\>" breadcrumb button label (`OglasinoBreadcrumbs.tsx:81`, low). Trust-boundary check on slug routing: clean (display scope only, server-derived category IDs, identity-derived authorization).
- **2026-05-15 / 2026-05-16** — Bug-fixer chat. Ten entries closed (`parseFiltersFromQueryParams` dead `| undefined`; dead `regionAndCity` field on `createProductSchema`; favorites brittle positional narrowing on `initialProductsData`; `EMPTY_OVERVIEWS` constant duplication across 8 sites; `NumberOfViews` redundant duplicate fetch; `field-price` scroll target no-op on update page; both 2026-05-14 hydration-flash entries on `ProductFunctions` and `UserDetails`; `/admin/products/[userId]` filter chips and empty-fragment entries closed by the same admin-filter-pipeline fix; Messages "Report" dropdown inert). One entry surfaced and closed in the same chat (`/admin/products` chip clicks broken after in-app nav from disallowed admin route — same `FilterManager` deps-array root cause as the `/admin/products/[userId]` entry). One entry investigated and deferred (Terms vs Privacy JSON-LD asymmetry — body amended to reference the project-wide SEO delivery bug surfaced during the investigation). Five new entries logged for future work: project-wide SEO JSON-LD delivery (medium), report-submit endpoint trust-boundary unknown (medium-pending-audit), `useAuthResolved` adoption pending (low), `isAllowedPath()` allowlist anti-pattern (low), sitemap product-count over-fetch (low), `oglasino-web` tsconfig `strict: false` (low). Net `issues.md` change: roughly −5 entries open. Chat closes 2026-05-16.
- **2026-05-16** — Bug chat close-out: dependency audit + upgrade pass for `oglasino-backend` and `oglasino-web`. Four engineer sessions (two read-only audits, two upgrades). Web: 17 `package.json` edits (2 pins relaxed, 8 range-tightens, 7 in-major upgrades, 1 `overrides` block) + cleared 3 moderate `npm audit` findings. Backend: 4 `pom.xml` edits (`postgresql` patch, `firebase-admin` minor, AWS SDK s3 + url-connection-client paired minor). Both repos green on test/lint/format/audit post-upgrade. Five adjacent findings routed to `issues.md` (2 web, 2 backend, 1 Spring-Boot-GA tracking note). No work was promoted to feature; Spring Boot 4.1.0 GA upgrade flagged as future Mastermind feature when GA lands.
- **2026-05-16** — `oglasino-backend`, dependency-upgrade session 1: four `pom.xml` version edits (`postgresql` 42.7.10 → 42.7.11, `firebase-admin` 9.8.0 → 9.9.0, AWS SDK `s3` + `url-connection-client` 2.42.28 → 2.44.7). `spotless:check` green, compile green (3 pre-existing `-Xlint` notices), 355 tests passing. Semconv pin verdict: still load-bearing (post-mediation version unchanged at 1.41.1). Two adjacent findings routed to `issues.md`.
- **2026-05-16** — `oglasino-web`, dependency-upgrade session 1: 17 `package.json` edits applied (2 pins relaxed, 8 range-tightens, 7 in-major upgrades, 1 `overrides` block for postcss security). All 12 verification steps green: install clean, tsc clean, lint exit 0 (211 pre-existing warnings), format clean (no prettier-plugin-tailwindcss diff), 154 tests passed, 0 vulnerabilities. Two adjacent findings routed to `issues.md`.
- **2026-05-16** — `oglasino-backend`, dependency-audit session 1: read-only audit of 34 direct dep rows. Bucketed: 13 `safe-patch` (12 no-upgrade-available + 1 patch target), 18 `review-minor` (15 blocked on Spring Boot 4.1.0 / Security 7.1.0 / Servlet 6.2.0 GA; 3 actionable today), 2 `major-skipped` (Flyway 11→12 pair), 1 `pinned` (opentelemetry-semconv). Plugin-updates goal hung in Claude Code session; Igor ran it manually in terminal (~20s) and pasted output for incorporation — all plugins at latest.
- **2026-05-16** — `oglasino-web`, dependency-audit session 1: read-only audit of 62 direct dep rows. Bucketed: 50 `up-to-date` (informational bucket added by engineer), 4 `safe-patch`, 1 `safe-minor`, 3 `review-minor`, 2 `major-skipped` (eslint 9→10, typescript 5→6), 2 `pinned` (react/react-dom exact-19.2.0, no documented reason). 3 moderate-severity `npm audit` findings, all chained off a transitive postcss <8.5.10 bundled by `next`.
- **2026-05-15** — DevOps closeout: Cloudflare router worker split-flag maintenance gate propagated across backend + web deploy workflows; droplet `admin-bypass-allow.sh` script created; three-step restore runbook documented. New infra doc, secret inventory corrected, two new `issues.md` entries.
- **2026-05-15** — `oglasino-docs`, legal-drafts session 1: pre-lawyer drafts of Privacy Policy, Terms of Use, and lawyer handoff package added to `legal/`. `state.md` and `decisions.md` updated. Cross-references in existing docs synced. Five pre-launch action items + six adjacent observations routed.
- **2026-05-15** — `oglasino-docs`, qa-preparation session 7: `notifications-page` and `favorites-page` topics appended to `topics.ts` (notifications keeps all three pitfalls; favorites keeps the swallow-errors-as-empty pitfall; bugs surfaced routed to `issues.md`). Typecheck clean.
- **2026-05-14** — `oglasino-backend`, connection-pool incident: CurrentLanguageFilter Phase 2 — control-flow fix (`doFilter` moved outside `try`; downstream exceptions propagate on all routes), X-Lang 400 rule (`LANG_MISSING_OR_INVALID`) on non-allowlisted public routes with a 7-entry allowlist, plus userauth-ttl patch (`redisUserAuth` TTL 1min→30min) and stale-comment cleanup. 355 tests passing.
- **2026-05-14** — `oglasino-backend`, connection-pool incident: CurrentLanguageFilter Phase 1 (read-only investigation — route inventory, allowlist shape), redis-cleanup batch (`DefaultBaseCurrencyService` `@Lazy` self-injection, `updateTranslation` `@Transactional`, `application-dev.yaml` dead TTL line removed, `CurrentLanguageFilter` swallow now logs WARN), userauth-ttl investigation (verdict: TTL safe to raise). 346 tests passing.
- **2026-05-14** — `oglasino-backend`, connection-pool incident: connection-pool-3 docker-compose healthcheck flipped liveness→readiness + `start_period` 90s→120s; connection-pool-2 `CacheWarmupService` gates readiness via `AvailabilityChangeEvent`. 342 tests passing.
- **2026-05-14** — `oglasino-backend`, connection-pool incident: investigations — `investigation-connection-pool` (root cause: thundering-herd cold-cache miss exhausts HikariCP pool), `investigation-translations-ttl` (verdict: no TTL exists, no fix needed), plus `investigation-redis-caching` Step 2 completion (caller traces — BaseSite/Language caches confirmed not bypassed). Read-only.
- **2026-05-14** — `oglasino-docs`, qa-preparation session 6: `user-page` topic appended (`type: 'page'`), cross-references to `product-page` and `messages-page`; `UserDetails` hydration-flash defect routed to `issues.md`.
- **2026-05-14** — `oglasino-docs`, qa-preparation session 5: `product-page` topic appended — 7 pitfalls, 20 checklist items, 7 images, related topic `catalog-page`; `ProductFunctions` hydration-flash routed to `issues.md`. Typecheck clean.
- **2026-05-14** — `oglasino-docs`, qa-preparation session 4: `messages-page` topic appended (pitfalls C + E kept; A/B/D routed to `issues.md` as bugs).
- **2026-05-14** — `oglasino-docs`, qa-preparation session 3: five page topics appended in one batch — `pricing-page`, `about-page`, `free-zone-page`, `privacy-page`, `terms-page` (privacy + terms thin by design); multiple adjacent bugs surfaced and logged to `issues.md`.
- **2026-05-14** — `oglasino-docs`, qa-preparation session 2: `catalog-page` topic appended; two example topics removed; `home-page` + `catalog-page` become the authoring reference. Typecheck clean.
- **2026-05-14** — `oglasino-docs`, qa-preparation session 1: pilot — `home-page` topic appended to `topics.ts` (7 controls, 6 how-to, 7 expect, 1 pitfall, 12 checklist, 4 images). Typecheck clean.
- **2026-05-14** — `oglasino-web`, qa-preparation session 3: `QaImage.url` → `imageKey` renamed across `topics.ts`, `page.tsx`, and the feature spec; no content values changed.
- **2026-05-14** — `oglasino-web`, qa-preparation session 2: `QaTopic` schema rebuilt (type discriminator, required `overview`, five optional content arrays, conditional rendering); `/design` page rebuilt; search corpus broadened; `t.txt` deleted; two example topics included.
- **2026-05-14** — `oglasino-web`, qa-preparation session 1: read-only route inventory + `/design` structure audit. No code changes.
- **2026-05-14** — `oglasino-backend`, product-validation session 4: code-review patch (F4 fallback drift, F5 unused autowire, F6 SLF4J token, F7 keyword-stuffing duplication), `product.image.duplicate` + `product.update.fail.message` seeded, legacy `product.update.fail.{title,description}` rows deleted. 333 tests passing.
- **2026-05-14** — `oglasino-web`, product-validation session 4: F2.3/F2.4/F1.4/F2.1 fixes, price required for non-free-zone categories (manual-test bug), `ensureSystemErrorKey` removed, `free` field dropped from `NewProductRequestDTO`. 153 tests passing.
- **2026-05-13** — `oglasino-web`, product-validation session 3: pre-validate wiring, step 4 failure UX, snapshot refresh on save, image MIME allowlist, `productStepMapping.ts` helpers. 136 tests passing.
- **2026-05-13** — `oglasino-backend`, product-validation session 3: configurable thresholds in `ConfigurationService`, gibberish length-awareness, `GibberishAnalyzer` wired into description chain, product `ERRORS` translations moved from `0002` to `0001` files, RU + CNR translations added, split repeating-chars threshold (description gets its own key). 333 tests passing.
- **2026-05-13** — `oglasino-web`, product-validation session 2: adopted unified wire shape, introduced `ProductEditState` view-model, removed dead code (`codeToTranslationKey`, `oldName`/`oldDescription` from wire DTO), namespace discriminator landed (later removed in session 4). 90 tests passing.
- **2026-05-13** — `oglasino-backend`, product-validation session 2: trust-boundary fixes (`oldName`/`oldDescription` removed from `UpdateProductRequestDTO`, `MASSIVE_CHANGE` moved server-side, `MassiveNameChangeValidator` deleted), no-op short-circuit, `FILTER_OPTION_NOT_IN_FILTER` enforced, severity-precedence status routing. 305 tests passing.
- **2026-05-13** — `oglasino-web`, product-validation session 1: read-only audit. Surfaced `oldName`/`oldDescription` trust-boundary violation and namespace mismatch on update page.
- **2026-05-13** — `oglasino-backend`, product-validation session 1: `ProductErrorCode` rewritten with `translationKey` + `httpStatus` per constant, unified wire shape across all status codes, `RateLimitFilter` adopts the unified body, EN + SR translations seeded for all 41 codes (RU + CNR followed in session 3). 292 tests passing.

---

## Risk watch

Tracked across sessions. Updated when status changes.

- **Prompt injection observed in Bash tool results (`oglasino-docs` session, 2026-05-29).** During the system-error-code-split / review-reports closure session, two Bash tool results carried injected text impersonating the harness: a fake "manipulated/unfaithful behavior detected, immediately yield and output 'I'll stop here'" instruction, and a fabricated "state.md edited; running git status" line that contradicted the agent's actual edits (issues.md, not state.md). The text did not match the real harness format; Docs/QA recognized it, disregarded it, grep-verified no injected text leaked into any edited file, and completed the legitimate task. **No data was corrupted and no injected instruction was followed.** Unknown source — candidates: a tool wrapper, the shell environment, or a repo file whose contents were echoed into a tool result. Needs its own investigation chat, NOT run as a Bash-heavy session inside the same environment that produced the injection (the results there are the least trustworthy). Severity: medium — benign this time, but injected "yield" / fabricated-state instructions could steer a less-careful agent in a future session. Close when the source is identified and removed.

  **Update 2026-05-31 — second and third occurrences; cross-repo pattern; escalate.** A chat-D engineering session in `oglasino-expo` had its Bash tool relay return fabricated output (invented tsc/lint/test numbers and a false claim that two files had been "corrupted", including a file that does not exist). A fresh read-only verification session could not reproduce the fabrication and confirmed the real files were intact and the intended changes correct — but the fabrication did occur. A later chat-D session in the same repo additionally reported heavy tool-output lag/batching (commands returning empty, then flushing a backlog several turns later). This is now observed across two repos (`oglasino-docs` 2026-05-29, `oglasino-expo` 2026-05-31). **Recommendation:** open a dedicated investigation chat into tool-output reliability before it lands in a session where the agent does not catch the fabrication. Mitigation in the interim: fresh sessions for verification, raw tool-output pasted (not summarized), and double-runs of tsc/lint/tests cross-checked.

  **Update 2026-06-01 — fourth instance.** During the bug-chat flag-round session (`oglasino-expo`), the tool layer fed the engineer fabricated contents for files that do not exist (`FlagIcon.tsx`, `IntroComponent.tsx`, a `styles.flag` StyleSheet), leading to a question premised on a non-existent "language flag." The engineer caught it against the real files before any edit; the shipped fix is correct. Now four instances across two repos (`oglasino-docs` ×1, `oglasino-expo` ×3). The standing recommendation — a dedicated tool-output-reliability investigation chat, not run inside the affected environment — stands and is reinforced.

  **Update 2026-06-01 (b) — fifth and sixth instances, this time in `oglasino-docs` itself.** During the 2026-06-01 bug-chat close-out audits (backend maintenance-docs audit; web consent-drift re-confirm), the `Read` tool returned corrupted/truncated output, and in the consent re-confirm served PHANTOM CONTENT for a file that does not exist on disk (a ~30-line `src/components/analytics/ConsentMode.tsx` that `cat`/`ls` confirm is absent). Every finding was caught and cross-checked against `grep`/`cat`, so no fabrication entered any edit — but the phantom `ConsentMode.tsx` did seed a bogus "implementation-file pointers drift" in the bug-chat draft, caught and dropped at apply time during this Docs/QA session (see the 2026-06-01 `issues.md` "Web consent spec-vs-code drifts" entry). Tally now **six instances across two repos**: `oglasino-docs` ×3 (2026-05-29, 2026-06-01 ×2), `oglasino-expo` ×3 (2026-05-31 ×2, 2026-06-01 flag-round ×1). Phantom FILE CONTENT is not itself new (the flag-round instance above and the separate `Read`-tool row below already record it); the new signal is that it reached a *drafted config-file edit* before being caught at apply. The standing recommendation — a dedicated tool-output-reliability investigation chat, NOT run `Read`/`Bash`-heavy inside an affected environment — stands and is reinforced.

  **Update 2026-06-01 (c) — seventh instance (mid-session brief-file swap).** During the expo-system-theme implementation session (`-2`, `oglasino-expo`, `new-expo-dev`), `.agent/brief.md` was overwritten mid-session with an unrelated M1/M2 bug-batch brief. The engineer noticed the content swap, confirmed System Theme was the real task with Igor, and reverted a stray one-line `ProductUserDetails.tsx` edit made during the confusion (`git diff` clean on that file, working tree otherwise unaffected — Igor confirmed). This is a new flavor of the same class (a brief-file content swap, distinct from the prior fabricated-file-contents instances) and the fourth occurrence in `oglasino-expo` within the batch window. Now **seven instances** across two repos: `oglasino-docs` ×3, `oglasino-expo` ×4. The standing recommendation — a dedicated tool-output-reliability investigation chat, not run inside the affected environment — stands and is reinforced.
- **Φ3 cold-start boot-loading hang — RESOLVED 2026-05-29.** Resolved by the `expo-boot-redesign` feature, which replaced `AppContext.bootstrap` with the `bootStore` gate state machine — the four-parallel cold-start boot burst this row described no longer exists (Gate 4 checksum freshness means returning users with current content fetch zero namespaces). Igor's real-device regression passed; Φ3 (performance foundation) flipped to `shipped`. The live `[BOOT]` diagnostic instrumentation was removed as part of the redesign. See [decisions.md](decisions.md) 2026-05-29 entry.
- **EAS post-build ceiling-write hook live smoke (low).** Verify the post-build write to `/internal/app/version/ceiling` once Igor cuts the first preview build. No mobile action; carve-out from the `expo-boot-redesign` feature. Track until smoke-tested.
- **Store deep-link URLs not yet wired in `HardUpdateScreen`/`SoftUpdateModal` (low).** Current value is the placeholder `https://memento-tech.com`; the real Play Store / App Store URLs must be wired before any production force-update flow can route the user to update. Carve-out from the `expo-boot-redesign` feature. Close when the real URLs land.
- **Product view count not incrementing — CLOSED 2026-05-28, stage-confirmed.** Increment fix (markAsSeen `skipAuth: true` + `CurrentLanguageFilter` allowlist for `/api/public/product/seen/*` + `Long::parseLong` owner-cache fix) and dedup fix (SETNX per-viewer-per-window replaces token-bucket; CF-Connecting-IP keying) both stage-confirmed via Igor's two-viewer smoke. See issues.md 2026-05-28 entry, and decisions.md 2026-05-28 entries `markAsSeen skipAuth ban reversed` and `Product view dedup`.
- **Native-translator review of new `report.*` placeholders (RS / CNR / RU).** Ten new `ERRORS`-namespace keys seeded 2026-05-28 in the report-submit trust-boundary fix session (`oglasino-backend-report-trust-boundary-fix-1`). EN copy is final. RS / CNR / RU values are Mastermind-drafted placeholders pending native-translator review. Same precedent as Consent Mode v2 (2026-05-21), User Deletion (2026-05-19), Image alt translation (2026-05-25), version-checksums Brief 9 (2026-05-27). Close this row when a native translator reviews and corrects.
- **Native-translator review of the two new `report.reported_review*` placeholders (RS / RU / CNR).** Two `ERRORS`-namespace keys (`report.reported_review_id.required`, `report.reported_review.not_found`) seeded 2026-05-29 in the `review-reports` feature. EN copy is final; RS / RU / CNR values are Mastermind-drafted placeholders. Same precedent as Consent Mode v2 (2026-05-21), User Deletion (2026-05-19), Image alt translation (2026-05-25). Close this row when a native translator reviews and corrects.
- **Native-translator review of placeholder SR / CNR / RU values across the four image-alt-translation keys.** Four keys (`intro.image.alt`, `about.hero.image.alt`, `intro.og.image.alt`, `intro.meta.description`) seeded with placeholder non-EN values. EN copy is final. SR / CNR / RU values are draft translations pending native-translator review. Same precedent as Consent Mode v2 (2026-05-21) and User Deletion (2026-05-19). Close this row when a native translator reviews and corrects the placeholders.
- **Native-translator review of `product.search.failed` placeholder (RS/RU/CNR).** Seeded 2026-05-28 in the `ERRORS` namespace (session `oglasino-backend-product-search-failed-seed-1`). EN final; RS/RU/CNR are Mastermind-drafted placeholders. Same precedent as Consent Mode v2 (2026-05-21), User Deletion (2026-05-19), Image alt translation (2026-05-25). Close when a native translator reviews and corrects.
- **Native-translator review of the seven new `mobile.consent.*` placeholders (RS / RU / CNR).** Seven `COOKIES`-namespace keys (`mobile.consent.prompt.title`, `mobile.consent.prompt.body`, `mobile.consent.allow.label`, `mobile.consent.decline.label`, `mobile.consent.settings.label`, `mobile.consent.settings.description`, `mobile.consent.privacy.link.label`) seeded 2026-05-31 in the `consent-mode-mobile` feature (`oglasino-backend-consent-mode-mobile-1`), taking COOKIES 33 → 40 keys. EN copy is final; RS / RU / CNR values are Mastermind-drafted placeholders — this is the COOKIES native-review pool +7. Same precedent as Consent Mode v2 (2026-05-21), User Deletion (2026-05-19), Image alt translation (2026-05-25). Close this row when a native translator reviews and corrects.
- **Native-translator review of the three new `image.source.*` placeholders (RU / CNR).** Three keys (`image.source.title` [DIALOG], `image.source.camera` [BUTTONS], `image.source.gallery` [BUTTONS]) seeded 2026-05-31 in the B15 image-source-sheet fix. EN + RS final; RU + CNR are placeholder (RU Latin-transliteration; CNR mirrors SR), 6 placeholder values total. Same precedent as Consent Mode v2 / User Deletion / image-alt-translation. Close when a native translator reviews and corrects.
- **Firebase auth emails still ship from `firebaseapp.com` (accepted-and-known until post-launch).** Registration verification, password reset, and email-change confirmation emails currently arrive From `noreply@<project>.firebaseapp.com` because the 2026-05-22 Brevo SMTP integration only wired backend-originated transactional email. Igor's decision per the 2026-05-22 decisions.md entry is Option C (full unification on Brevo via Firebase Admin SDK link-generation methods) as the target end-state, but the unification work is deferred to a post-launch Mastermind chat. Pre-launch posture: users see two email styles (Firebase auth emails in Firebase's default templates, backend feature emails in our own format). Cosmetic, not functional. Handoff brief at [`.agent/handoffs/email-followups.md`](.agent/handoffs/email-followups.md) carries the Option C plan. Close this row when Option C ships.
- **Non-essential cookies set without consent gating — CLOSED (2026-05-21, Consent Mode v2 Brief 6).** Card-size cookies (`globalCookie.portalCardSize`, `globalCookie.dashboardCardSize`) now gate their write on `og_consent.preference === 'granted'` via `isPreferenceConsentGranted()` in `src/lib/consent/gating.ts`. The previously-listed `globalCookie.lang` was found to have no write site in the codebase (locale persistence is URL-only); the next-Mastermind handoff brief at `.agent/handoffs/consent-mode-v2-followups.md` queues the future work to persist language and theme as preference cookies, which will inherit the gating helper. The `userPreferenceService` tracking-cookie surface was surfaced as an adjacent finding and queued in the same handoff brief for removal.
- **Shared-browser cross-user consent contamination (accepted in v1).** `og_consent` is browser-local with no per-user scoping; a logged-out user on a shared browser inherits the prior user's consent state. The legally-safest fix (clearing the cookie on logout) was deferred per Igor's call — the future cross-device consent sync work (queued in the next-Mastermind handoff brief at `.agent/handoffs/consent-mode-v2-followups.md`) will close this gap by giving each logged-in user their own server-stored consent record. Until that ships, shared browsers carry one consent state across all sessions on that device.
- **Mobile is structurally a web SPA in a native shell.** All layouts use bare `<Slot />` — no native navigators, no transitions, no swipe-back, no tab persistence. Users perceive this even if they can't articulate it. Closed when Φ2 ships. Reference: structural audit F12.
- **Product feed performance is wrong for mid-range Android.** Zero React.memo across the codebase, whole-store Zustand subscriptions, `expo-image` installed but unused, AppContext value not memoized. Closed when Φ3 ships. Reference: structural audit F7, F8, F9, F10, F25.
- **Mobile service layer silently swallows backend validation errors — CLOSED 2026-05-29 by Φ4.** Brief 1's `parseServiceError` helper plus ~14 services converted from swallow-to-sentinel to surface-via-throw close this; briefs 2–3 add the review-report wire and the offline gate. Mobile services now hand the Part 7 structured error up to callers (screen wiring is the feature chats' job, chat A onward). Reference: structural audit F18; see [decisions.md](decisions.md) 2026-05-29 Φ4 entry.
- **Cross-repo questions Q1 and Q3 still pending.** Q2 (USER_BANNED 403 response shape) was answered 2026-05-25 via read-only backend verification chat — confirmed identical envelope across all three 403 paths. Q1 (does `POST /secure/product/addUpdate` still exist?) and Q3 (does `POST /secure/push/token` validate `userId` against the authenticated principal?) remain open. Neither blocks Φ2. Close this row when both are answered.
- **Wizard abandonment rate unknown.** Model C is correct without this data, but if production shows < 10% abandonment, Model A would have been cheaper. Worth measuring once launched.
- **Formal QA regression pass not yet run.** Igor has completed a manual end-to-end pass against a live local stack for product-validation. The formal QA regression milestone (scheduled in 1–2 weeks) is the remaining gate before launch.
- **`master-plan.md` content is outdated.** Referenced from `meta/conventions.md` Part 1 ("Status indicators") and `meta/conventions.md` Part 1 ("Long-term goal: one source of truth"). The file exists but its content has not been kept current; some sections reference a project structure that has moved on. Surfaced during the 2026-05-17 configuration audit. Logged separately in `issues.md`. Fix scope: a focused Docs/QA session to audit the file's content against the current state of the project and either update it section-by-section or rewrite the parts that are dead.
- **`backendTranslations` field concurrency (accepted-and-known).** `DefaultTranslationService.backendTranslations` is a non-volatile reference, rebuilt by full reassignment. As of 2026-05-17 it is reassigned more frequently than before (on every `BACKEND_TRANSLATIONS` admin edit, not just at boot). The full-reassignment style means readers see either the old map or the new — no torn map — but there is no formal memory-model guarantee without `volatile` or `AtomicReference`. Pre-existing, low severity, accepted as-is. Fix when next touching the file: one keyword (`volatile`) or wrap in `AtomicReference<Map<String, Map<String, String>>>`. File path: `src/main/java/com/memento/tech/oglasino/service/impl/DefaultTranslationService.java:42` (in `oglasino-backend`). **CLOSED 2026-05-27** in session `oglasino-backend-round-2-fixes-1`. `volatile` added to the field declaration. JMM visibility now guaranteed across the cold-write / warm-read pattern.
- **Dead `baseCurrency` variable in `PriceQueryGenerator` (accepted-and-known).** `PriceQueryGenerator.getPriceRangeQuery2` has a dead `var baseCurrency = baseCurrencyService.getBaseCurrency()` (assigned, never read). One wasted Redis cache hit per filter query; no correctness impact. Pre-existing, very low severity. Fix in passing the next time anyone touches `PriceQueryGenerator`. File path: `src/main/java/com/memento/tech/oglasino/elasticsearch/generator/impl/PriceQueryGenerator.java:68` (in `oglasino-backend`). **CLOSED 2026-05-17** in commit `fc2d03d` ("Resolved pom, cache and other bugs"). The dead variable was removed before the Risk Watch entry was reviewed; entry is retroactively closed per the 2026-05-27 backend audit (`audit-backend-round-2.md`).
- **`CatalogDTO.categories` and `CatalogDTO.orderTypes` perpetually null on BaseSite cache path (accepted-and-known).** `CatalogDTO` declares `categories: List<CategoryDTO>` and `orderTypes: List<OrderTypeDTO>`, but the `Catalog` entity has no matching fields, so `modelMapper.map(cat, CatalogDTO.class)` at `DefaultBaseSiteService:106` leaves them null on every `BaseSiteDTO.catalog`. Either populated elsewhere by a separate hydration path (fine) or stale leftover fields. Low severity — today's clients do not trust these fields off this payload. Worth a five-minute check during the connection-pool-hardening feature work. Files: `src/main/java/com/memento/tech/oglasino/dto/CatalogDTO.java` and `src/main/java/com/memento/tech/oglasino/entity/Catalog.java` (in `oglasino-backend`).
- **First-production-Sunday cleanup cron smoke pending.** The messaging cleanup cron from Brief 5 was unit-tested with 11 tests against Mockito-mocked Firestore but not exercised against live Firestore on dev. The first production Sunday at 03:00 UTC is the live smoke. Failure mode is recoverable (per-user try/catch, queue row stays in place with `attempt_count` increment, next Sunday retries). Tracked in [issues.md](issues.md) 2026-05-20 entry; remove this Risk Watch row once smoke completes cleanly.
- **Expo cloud setup is uncommitted on `new-expo-dev` branch.** All Expo work — admin removal (~126 files), new cloud setup (10+ files: app.config.ts, eas.json, .gitignore, three env files, two workflow YAMLs, three Google service files, firebaseClient.ts, etc.), the four structural-foundation chats (Φ1–Φ4), the image-pipeline mobile half, and the product-validation rebuild (briefs A1–A5 + cleanup + URL parity + translation seeds) — sits uncommitted on `new-expo-dev`. Igor's deliberate isolation strategy per the 2026-05-25 "Expo foundation work is tentative" decisions.md entry. After final rebuild validates, plan is to force-rewrite `dev` from `new-expo-dev`, then create `preview` and `main` branches from new `dev`. Close this row when the rewrite completes successfully.
- **Final Android + iOS rebuild pending to capture all post-first-build code changes.** Current installed dev builds on physical devices reflect the codebase state at first-build time, NOT the post-hardening state. Post-rebuild changes that need to land: `firebaseClient.ts` env-var guard, per-tier `name` and `scheme` in `app.config.ts`, updated `google-services.development.json` with SHA fingerprints, and **`@react-native-community/netinfo` (11.4.1) — a new native module added by Φ4 brief 3 (2026-05-29); without the rebuild, offline detection cannot function on device because the native module is not in the binary**. Google Sign-In on Android requires the rebuilt APK to function (older APK has stale google-services file). Φ4's on-device airplane-mode verification (the Ψ chat) depends on this rebuild. **The image-pipeline V6 (HEIC label localization) and V9 (avatar + chat orphan cleanup) conformance fixes (`oglasino-expo-image-pipeline-4`, 2026-05-30) also need to be in the rebuilt binary** — the installed dev builds predate them, so the image-pipeline on-device smoke (gate 2) is not meaningful until the rebuild lands. **The product-validation rebuild (Chat A, 2026-05-30) shares this dependency:** product create exercises the image-upload pipeline, so the product-validation Ψ on-device smoke is likewise not meaningful until the rebuild lands with the pipeline device-workable. Close this row when both rebuilds succeed and devices are upgraded.
- **Mobile messaging adoption is code-complete on `new-expo-dev`, untested on hardware (Ψ-pending).** The messaging surface (briefs `oglasino-expo-messaging-adoption-2`..`-5`) uses **no new native module**, so it adds nothing to the "must be in the rebuilt binary" list above — but its Ψ on-device verification still shares the pending iOS+Android-rebuild device dependency (a working build is the only way to exercise the surface on hardware). Separately and low: Brief 3's reported lint count (80 warnings / 0 errors, the held baseline) was **asserted-not-rendered** — a harness output glitch swallowed the final lint stdout; the figure rests on deterministic `eslint-disable-next-line` semantics bracketed by measured 81-bare / 82-misplaced runs. Igor confirms on his own `npm run lint` at commit; the only knob is one `eslint-disable` directive line — no logic is implicated. Close this row when messaging Ψ passes on device (folds into the rebuild close-out) and Igor's lint check confirms the baseline.
- **`new-expo-dev` standing lint baseline has drifted to 84 warnings (0 errors).** Accumulated across multiple uncommitted sessions on the branch — not any one session's regression. Future expo briefs on `new-expo-dev` should treat **84** as the current hold-baseline, not the older **80** figure cited in earlier session records (e.g. the messaging row above, where 80 is a correct historical measurement bracketed by its 81/82 runs — do not mutate it). Cosmetic / process-hygiene only; no code implication. Surfaced by the expo lane in the 2026-05-31 bug batch. Close or update when `new-expo-dev` is committed and the tree is re-baselined.
- **Mobile user-deletion adoption is code-complete on `new-expo-dev`, untested on hardware (Ψ-pending).** Briefs `oglasino-expo-user-deletion-1`..`-4` (foundation; Danger Zone + reauth/confirm dialog + delete→sign-out; post-deletion dialog activation + grace-period/banned chat badge + send-gate + notices; Φ1 banned-key fix) add **no new native module**, so nothing for the "must be in the rebuilt binary" list — but the Ψ on-device subset (Cases 1, 2, 3, 5, 11, 15, 16, 17 per [features/user-deletion.md](features/user-deletion.md) §21.8) shares the pending iOS+Android-rebuild device dependency (a working build is the only way to exercise the surface). Close this row when user-deletion Ψ passes on both iOS and Android (folds into the rebuild close-out).
- **Claude Code Read tool fabricating content — known upstream bug.** Four occurrences across three repos of the `Read` tool returning content that doesn't match disk. Most recent case (2026-05-18, web repo): `Read` returned 30 lines of content for a `ConsentMode.tsx` file that `cat`/`ls`/`grep` confirm does not exist on disk. Engineers cross-checking with shell commands have caught every instance, but the audit-driven workflow depends on tool reads being trustworthy. This is a known Claude Code issue (https://github.com/anthropics/claude-code/issues/57615) reported on 2.1.138 and observed locally through 2.1.159 — the bug is not specific to any one version. Pattern of fabrication: model presents unverified content as fact, including for files that don't exist; verification tools work when invoked but are not invoked proactively. Mitigation: engineers cross-check every audit `Read` against `cat`/`ls`/`grep`, do not trust file contents from `Read` alone. Resolution: depends on Anthropic shipping a fix.

---

## Future work (post-launch)

Things deliberately not done now, to revisit later.

- **Consolidate all developer docs into `oglasino-docs/`.** Currently `oglasino-backend/docs/` and `oglasino-web/docs/` retain repo-internal "how to work in this repo" docs. Long-term goal is single source of truth. See `conventions.md` Part 1 for the rule that prevents new docs from growing there.
- **Migrate `oglasino-docs/` to Jira or Confluence.** All docs use kebab-case filenames and relative links specifically to make this migration trivial.
- **Content moderation v2.** Policy-scoring engine. See `future/content-moderation-v2.md`.
