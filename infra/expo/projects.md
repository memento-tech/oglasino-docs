# Expo Projects

A single Expo project covers all environments (dev / stage / prod) via
EAS build profiles — see
[`eas-configuration.md`](eas-configuration.md).

## oglasino-expo

- **Expo project name:** TBD
- **Slug:** TBD
- **Workspace ID / owner:** TBD
- **GitHub repo:** `oglasino-expo`
- **applicationId / bundle identifier:** `com.oglasino` (single ID
  across environments; app names differentiated per profile via
  `app.config.ts`)

Populated during Phase 3E.1–3E.2 of
[`../master-plan.md`](../master-plan.md).
