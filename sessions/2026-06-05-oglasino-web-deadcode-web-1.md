# Session summary

**Repo:** oglasino-web
**Branch:** dev
**Date:** 2026-06-05
**Task:** Inventory dead/unused code (read-only). For every candidate: file:line, what it is, evidence it's unused. Bucket into Tier 1 (safe to delete) / Tier 2 (looks dead, has a catch) / Tier 3 (intentionally kept). No fixes.

## Implemented

- Read-only audit only. Wrote the full inventory to `.agent/audit-deadcode-web.md`.
- Ran `npx tsc --noUnusedLocals --noUnusedParameters` (CLI flag override, no tsconfig edit) and `npx knip@5` (runs from npx cache, no repo writes, no config added) as discovery signals, then **hand-verified every knip candidate** with exact-path/in-file grep. Verification overturned several knip calls (KeyLabelPair, sonner/textarea consumers, the nextCalls product fns, sharp/tailwind-scrollbar-hide/typescript-eslint deps, the whole dynamic-icon family).
- Result: **Tier 1 = 19 items** (9 orphan files, 7 dead exported symbols, 2 dead types, 1 dead local var); **Tier 2** = dynamic-icon family + vendored shadcn kit + ~35 over-exported-but-locally-used types + 8 internal-only over-exports + 2 test-only symbols + ~30 vitest files + 3 build/tooling files + 3 false-positive deps; **Tier 3 = 1** (`FacebookIcon.tsx`, retained per issues.md wontfix).
- Brief-specific checks all completed: `isAllowedPath` confirmed gone; `shown` notification member confirmed absent web-side (expo-only residue); FilterManager exhaustive-deps left out of scope; unused i18n keys skipped (not cheaply determinable — keys seeded from backend SQL, consumed dynamically); no dead CSS modules (Tailwind + single globals.css).

## Files touched

- `.agent/audit-deadcode-web.md` (new, +~150 lines) — the deliverable.
- `.agent/2026-06-05-oglasino-web-deadcode-web-1.md` + `.agent/last-session.md` (this summary).
- No source files modified (read-only brief).

## Tests

- Ran: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` (discovery only) → 3 unused-local hits (free-zone `v`, two `generateCatalogPageMetadata` params).
- Ran: `npx knip@5 --no-progress` (discovery only).
- Did **not** run `npm test` / `npm run lint` — no code changed, nothing to validate. (Per Part 4, those gates apply to "touched paths"; no source path was touched.)
- New tests added: none (read-only).

## Cleanup performed

- none needed (read-only audit; no code touched).

## Config-file impact

- conventions.md: no change.
- decisions.md: no change.
- state.md: no change.
- issues.md: no change. (The audit's Tier 1 list is a candidate source for future issues.md entries, but per Part 3 I do not write it — drafting note in "For Mastermind" below.)

## Obsoleted by this session

- Nothing. This session produces an inventory; it deletes nothing and obsoletes no existing code. (It *identifies* 19 Tier-1 deletion candidates for a future cleanup brief.)

## Conventions check

- Part 4 (cleanliness): confirmed — no code added, no debug logging, no TODOs, no new unreferenced source files (the one new file is the briefed audit deliverable).
- Part 4a (simplicity): see structured evidence in "For Mastermind".
- Part 4b (adjacent observations): flagged in "For Mastermind".
- Part 6 (translations): N/A this session (no translation keys added; unused-key analysis deliberately skipped per brief as not cheaply determinable).
- Other parts touched: Part 5 (session-summary discipline) — followed; `<n>`=1 (no prior `*-deadcode-web-*.md`).

## Known gaps / TODOs

- **i18n unused keys not analyzed** — deliberately skipped per the brief's escape hatch; web has no local message catalog (keys seeded backend-side, consumed dynamically), so a web-only unused-key set isn't cheaply derivable.
- **Auto-generated icon index caveat** — Tier 1 items #7–#9 (OtherIcon/PressureRangeFilterIcon/WeightFilterIcon) are orphaned today but `dynamic/index.ts` is generated by `scripts/convert-icons.js`; confirm the icon source manifest before deleting in case the generator would re-emit them.
- **`letItStay` name ambiguity** — Tier 1 #16 is genuinely unreferenced but its name reads like a "keep me" marker with no backing comment/decision. Flagged in the audit for Igor to confirm before any deletion.

## For Mastermind

- **Part 4a simplicity evidence (required):**
  - Added (earned complexity): nothing — read-only audit, no code or abstractions introduced.
  - Considered and rejected: did **not** add a `knip.json` config to the repo (would be a code change outside the read-only brief, and the audit only needed knip's raw candidate list, which I verified by hand). Did **not** install any dev dependency — used `npx`-cached tools instead.
  - Simplified or removed: nothing (no code changed).

- **Highest-value Tier 1 candidates for a future cleanup brief** (all verified zero-usage): orphan files `nextCalls/favoritesService.ts`, `popups/components/FilterSelector.tsx`, `types/configuration/RegexData.ts`, `types/KeyLabelPair.ts`, `shadcn/ui/sonner.tsx`, `shadcn/ui/textarea.tsx`, three orphan dynamic icons; dead symbols `useConfig`, `pathnameWithoutLocale`, `firebaseClient.storage`, the two nextCalls `get{Dashboard,Admin}ProductDetails` wrappers, the duplicate `getColorForLetter` in `AuthUserProfileButton`; dead types `ReportDialogProps`, `CardStylings`. These are clean deletes; a single `chore/deadcode-web` brief could land them with one tsc/test pass.

- **Part 4b adjacent observations** (noticed during audit; not fixed — out of read-only scope):
  - **Duplicate type definition** — `KeyLabelPair` is declared both in `src/lib/types/KeyLabelPair.ts` (orphan) and inline in `src/lib/types/catalog/RegionAndCityDTO.ts:1`. Severity low. file: both above. Not fixed (out of scope).
  - **Stale props type** — `ReportDialogProps` (`ReportDialog.tsx:25`) is out of sync with the component's actual props (lists `reportedProductId/UserId/ReviewId` but omits `isOpen`/`onClose` that the component destructures). Could mislead a future reader. Severity low-medium. Not fixed (out of scope).
  - **Duplicate avatar-color helper** — `getColorForLetter` is copy-defined in both `AuthUserProfileButton.tsx:34` (dead) and `OglasinoAvatar.tsx:4` (live). A future change to one won't track the other. Severity low. Not fixed (out of scope).
  - **Test-only survivors** — `deleteOgConsent` (`consent/cookie.ts:47`) and `stepForField` (`utils/productStepMapping.ts:23`) have no production caller; only their unit tests keep them alive. Decide per symbol whether the capability is still wanted or the function+test should go together. Severity low.
  - **Over-export pattern** — ~35 exported types and ~8 exported functions are used only within their own module (the `export` keyword is redundant). Not dead code; a stylistic cleanup at most. Listed in the audit Tier 2. No action recommended unless a lint rule is wanted.

- **Adjacent (non-code) — unused npm dependencies** (verified zero refs): `@radix-ui/react-accordion`, `@tanstack/react-virtual`, `react-syntax-highlighter`, devDeps `@types/file-saver`, `@types/js-cookie`. Removal is a `package.json` change outside this brief; routing to Mastermind to decide if a deps-prune chore is wanted. **Do not** remove `sharp` (Next.js runtime image optimization), `tailwind-scrollbar-hide` (used via `scrollbar-hide` in globals.css), or `typescript-eslint` (eslint.config.mjs) — knip false positives, confirmed in-use.

- **Config-file drafts:** none required this session. The Tier 1 list is inventory for a future brief, not an issues.md entry I should author (Part 3 — Docs/QA is the sole writer; if Mastermind wants these in issues.md, the draft would originate from a Mastermind/bug chat).
