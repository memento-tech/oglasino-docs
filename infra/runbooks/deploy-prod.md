# Runbook — Deploy to Prod

TBD — written during Phase 5.2 of
[`../master-plan.md`](../master-plan.md).

Will cover: how to promote `stage` → `main` (prod), pre-deploy
checklist, monitoring during deploy, post-deploy smoke tests, rollback
procedure if something goes wrong.
