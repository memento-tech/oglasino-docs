# Runbook — Deploy to Stage

TBD — written during Phase 5.2 of
[`../master-plan.md`](../master-plan.md).

Will cover: how to push a feature through `dev` → `stage`, what CI
steps fire, how to verify the deploy on stage, what to check on the
droplet, where logs live, rollback procedure.
